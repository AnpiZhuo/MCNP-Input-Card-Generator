# 项目记忆文档（AI 速查手册）
> 最后更新时间：2026-08-15（🛑 停止自动化发布，转手动打包文档；下次打包版本 = **1.7.0**）
>
> **✅ P0 修复完成：3D 结果窗口渲染整棵主应用树（frontend，2026-08-15，测试先行）**：`main.rs:85` `create_or_focus(&app, "volume3d", …)` 建窗 label 改 **"volume3d"→"volume"** 单值统一（App.tsx:444 已路由 `volume`→ResultWindow，无需改 App.tsx；调试 hash `#/volume` 与真实窗口 label 从此同值，对齐 preview3d/cross_section 惯例；command 名 `open_volume3d_window`/title/尺寸不变）+ `create_or_focus` 重复 `if let Some(win)` 块清一段（两段逐字节相同，PM 批准）。完整性核验：全仓 `volume3d` 引用唯一窗口 label 是 main.rs:85；`windows.ts` 桥 key `mcnp_win_volume3d`（KEY_VOLUME3D）与 `invoke("open_volume3d_window")` 保持不动；tauri.conf.json 无 volume3d 定义。**测试先行**：新增 `gui/test/volume/windowRouteConsistency.test.ts` 3 用例（main.rs 建窗 label 集合 == App.tsx 路由分支 label 集合 / 三窗 label 同值绝无 volume3d / 桥 key 守卫），先红（2 失败：set 不等 + main.rs 缺 volume）后绿。**vitest 206/0**（203 基线零回归 + 3 新增，唯一偶发红=colorize 128³ 计时已知 flaky 隔离跑通过）+ tsc EXIT 0 + build EXIT 0 + **cargo check EXIT 0**（mcnp-ui v1.7.0 Finished dev 29.73s）。改动记 docs/frontend-changes.md。未 commit（PM 统一提交，P0 落库后派 packager 重打包 + 3D 结果窗口专项冒烟）。
>
> **🔍 P0 根因排查记录（已被上方 ✅ P0 修复完成条目取代，2026-08-15）**：用户反馈「网格计数 3D 结果会弹出一个客户端」。只读排查（零改动）确认为**真 bug**：`gui/src-tauri/src/main.rs:84-86` open_volume3d_window 经 create_or_focus 建窗 **label="volume3d"**，但 `gui/src/App.tsx:434-446` WindowRouter 只认 `label === "preview3d"/"cross_section"/"volume"`（"volume" 对应浏览器调试 hash `#/volume`），**无 volume3d 分支** → 路由不匹配落 `<DeckProvider><AppInner/></DeckProvider>` → volume3d 原生子窗口（标题「3D 结果」1300×820）渲染整棵主应用 UI（用户看到的「客户端」）。**排除**：非浏览器（Tauri invoke 成功→原生窗）、非第二 OS 进程、非重复开窗（create_or_focus 复用）。对照另两窗 label 与路由一致（preview3d/cross_section），**唯独 volume3d 例外，自 ea20ad7 引入从未修复**；已部署版（fcc935b 与 38729c9 重打包版）均带此 bug。**修复（上级拍板，frontend 施工中）**：main.rs:85 label "volume3d"→"volume" 单值统一（App.tsx 已路由 "volume"，对齐 preview3d/cross_section 惯例）+ 窗口 label↔路由一致性回归测试（vitest 源码级守卫）+ 顺手清 main.rs:51-60 create_or_focus 重复 `if let Some(win)` 死代码。**⚠️ 与重打包联动**：38729c9 重打包版已部署（**不含此修复**），P0 落库后需重跑打包再部署（frontend 改动 → vite + tauri 重编译 + 3D 结果窗口专项冒烟）。未 commit，等修复完成统一提交。
>
> **✅ v1.7.0 重打包部署完成（tester，2026-08-15，HEAD 38729c9，无 commit）**：两批用户实测修复带进已部署版。全流程：门禁 pytest 461/0 + vitest 203/0（fmeshNormalize 6 用例全绿）→ vite build（index-2Jocuteb.js 899KB）→ PyInstaller sidecar 25091368B（spec 核对 meshtal+preview_cache 无回退）→ binaries → tauri build 11.67s → **6.2 时效校验命中增量坑已手动覆盖**（target/release 旧 25088008B/13:12 → 新 25091368B/14:26）→ 部署。冒烟 5 项全过：① 5001 探活 `xsdir loaded:true`（count 7621）；② meshtal 3 端点不挂起（detect 0.002s / parse 0.38s / texture 0.39s）；③ 官方 case1~5 parse-inp fmesh_defs 完整（particle='p'/geom='xyz'/out='jk' 后端原文保留，前端 fmeshNormalize.test.ts :52-59 断言 fmeshDefsToRows 归一化为 P/XYZ/JK，6 用例全绿）；④ 真实 jk meshtal parse status ok + `warnings:[]`（无 pymcnp 误导警告，Bug 2 修复生效）+ tally14/p/1×2×2 + grid_bounds min[49,-10,90] max[51,10,110] + texture ok（resolution[1,2,2]）；⑤ 版本 1.7.0 双信号（exe FileVersion=1.7.0 + bundle 含 1.7.0，部署 exe sha256 e4afb252 与 target/release 一致）。部署 D:\MCNP\MCNP输入卡生成器（exe 6389760B/14:26:45 / python.exe 25091368B/14:26:45 / _internal 完整：preview_cache+geouned+app/meshtal 9 项）。版本四处已一致 1.7.0 无版本 commit。环境已清理（5001 空闲）。遗留：真浏览器 #/volume 下拉 P/XYZ/JK 人工冒烟待补（非阻塞）。bundle grep 前端函数名因 minify+base64 嵌入不可行，改以构建输入链+单测+源码证据验证。
>
> **✅ 用户实测双 Bug 全链路修复完成（PM 汇总，2026-08-15，feat/meshtal-volume）**：官方 FMESH 测试文件实测暴露两 bug 全部闭环——**Bug 1（frontend）导入下拉不更新**：particle/out 导入值小写未归一化 vs 下拉 option 大写，fmeshState.ts 新增 `normalizeParticle`/`normalizeOut` 并统一应用两条导入链路，**vitest 203/0**（197 基线 + 6 新增）；**Bug 2（backend）MESHTAL 解析失败**：用户真实 meshtal 为 out=jk 二维矩阵布局，原解析器只认 col 逐体素行，meshtal_parser 新增 `_parse_matrix_data`（ij/ik/jk 通用 + col 列头识别 + Energy/Time 帧 + Total 段跳过），收尾删 pymcnp 死代码校验（成功解析 warnings 恒空）+ manifest 缓存 v2 失效旧版本，**pytest 461/0**（454 基线 + 7 新增：matrix 5 + warning/cache 2）。tsc/build EXIT 0。回归样本：官方 case1~5.i + 用户真实文件 `tests/fixtures/real_meshtal_jk.meshtal`（原样复制）+ real_meshtal_{ij,ik,col,jk_multi}。契约/api JSON key 零改动。**统一提交进行中（Bug1+Bug2 一次 commit，未 push）**。**建议重打包 v1.7.0**：当前已部署 fcc935b 版即用户实测命中两 bug 的版本，重打包须含 f39a218 等号可选解析修复 + 本批（含 manifest v2，自动使旧磁盘缓存失效）。
>
> **✅ Bug 2：MESHTAL 解析失败修复（backend，2026-08-15，feat/meshtal-volume）**：PM 派发，用户实测官方 case（fmesh14:p out=jk）跑完解析 MESHTAL 报「文件格式不对或版本不兼容」。**根因实证**：用户真实文件 `D:\MCNP\new\claude\meshtal` 是 out=jk **二维矩阵布局**（`Tally Results: Y (across) by Z (down)` + 列头 + 数据行），原 `_parse_tally_block` 只认 col（每体素一行）→ 矩阵行全跳过 → 抛「未解析到数据行」。**附带发现**：① `from pymcnp.meshtal import Meshtal` 恒 ImportError（pymcnp `meshtal/__init__.py` 只导出 Block/Header/Tally，Meshtal 类在 `src/pymcnp/Meshtal.py` 大写 M）→ pymcnp 路径从不生效、实际一直走轻量兜底；② 真实 out=col 单 bin 显式能量边界仍打印 Energy 列（6 列），原 `has_energy_col=energy_bins>1` 判 5 列 → col 也挂；③ `.msht` 文件头多数无 `mcnp version` 横幅 → 原 header 解析把标题首词当 code。**修法**（app/meshtal/meshtal_parser.py）：col 改按列头列名识别 + 新增 `_parse_matrix_data`（Energy/Time Bin 帧标签下边界→bisect、`<C> bin:` 固定轴、`Tally Results: A (across) by B (down)`、列号==across bin 序号、Total Time/Energy Bin 聚合段跳过防重复）+ `_parse_tally_block` 布局判别分派 + `_parse_global_header` 横幅感知。**测试先行**：tests/unit/test_meshtal_matrix.py +5（用户 jk / ij / ik / col≡jk 等价 / 2E×2T 4 帧 Total 跳过），先 5 红后全绿；vendor 5 个真实 MCNP6 输出 fixtures（tests/fixtures/real_meshtal_{jk,ij,ik,col,jk_multi}.meshtal，用户文件原样复制），**全量 pytest 459/0**（454 基线零回归）。端到端 worker parse/texture 用户真实文件全 ok（tally14/p/1×2×2/grid_bounds 49,-10,90~51,10,110）。改动记 docs/backend-changes.md §N。契约/api JSON key 零改动。simplify 单趟已跑（抽 `_get_frames` + 矩阵 i/j/k 位置字典简化）。**收尾（PM 决策，2026-08-15）**：成功解析不再 emit 误导性「pymcnp 解析失败」警告（删死代码 pymcnp 校验 + warnings 恒空，真失败走异常传播）+ manifest 缓存版本号 `_MANIFEST_VERSION=2`（meshtal_cache.py）使旧版磁盘 manifest 失效重解析（防旧警告被缓存投放）；tests +2（test_meshtal_parser.py 成功解析 warnings 空 / test_meshtal_cache.py 旧版 manifest 失效），先红后绿，**pytest 461/0**。worker 真实文件 parse → `warnings:[]`，前端「已解析」不再带「警告 N 条」。docs/backend-changes.md §N.7。未 commit。
>
> **✅ Bug 1：FMESH 导入下拉字段不更新修复（frontend，2026-08-15，feat/meshtal-volume）**：用户实测官方 case（`fmesh14:p geom=xyz out=jk`）导入后粒子设计符/GEOM/OUT 下拉不更新。根因 = 导入值小写、下拉 option 大写 → React select 受控 value 不匹配任何 option → 空白（geom 已有 `normalizeGeom` 归一化，particle/out 未归一化）。修复（fmeshState.ts）：新增 `normalizeParticle`（大写单字母，MCNP 大小写不敏感）+ `normalizeOut`（大小写不敏感匹配 `FMESH_OUT_OPTIONS` 命中转大写、未知值保留原文保 round-trip）；`cardTextToFmesh` 卡头 particle 归一化 + OUT 分支归一化；`fmeshDefsToRows`（INP 导入主链路）particle/out 归一化。生成路径 `fmeshToCardText`/`cardLines`/`buildFmeshPayload` 零改动（解析已归一化→生成自然输出大写 MCNP 写法，round-trip 稳定）。**测试先行**：新增 `gui/test/volume/fmeshNormalize.test.ts` 6 用例（纯函数 / 官方 case1.i 卡体原样回归 / 后端 parse 载荷 / round-trip / case2~5 `inc=` 变体），先红（6 失败）后绿。**vitest 203/0**（197 基线零回归 + 6 新增，唯一偶发红=colorize 128³ 计时已知 flaky 隔离跑通过）+ tsc EXIT 0 + build EXIT 0。改动记 docs/frontend-changes.md。未 commit。
>
> **✅ FMESH 关键字等号可选解析修复（backend，2026-08-15，feat/meshtal-volume）**：PM 派发，官方测试暴露——MCNP 允许空格分隔 `imesh 51`、等号可选；原 `_KEY_RE` 强制 `=` 导致 `imesh` 被当未知 token 跳过、网格字段空。修复（app/meshtal/fmesh_parser.py）：①`_KEY_RE` 改 `^([A-Za-z]+)(?:=(.*))?$`（等号可选）；②新增 `_is_boundary_token`（:32-41）——收集循环 break 由「任意字母词」改为「卡族头 / 已知关键字（大小写不敏感）/ 任何 `key=` 形未知关键字」，防 `geom xyz` 的 `xyz` 值被误断截断、防 `out=jk inc= 0` 的 `inc=` 被 out 值吞并；③未知关键字 `inc=` 跳过不报错 + 整段记入既有 `raw` 字段兜底（round-trip 不丢，`_card_lines` :228-234 追加回放）；JSON key 不变（imesh/iints/.../factor/geom/origin）。**测试先行**：tests/parser/test_fmesh_parser.py +5（空格分隔 / 等号+大小写 / 混排+geom xyz 不截断 / inc= 容错+raw round-trip / 官方 case1~5.i vendor→网格字段非空）+ vendor 5 fixtures（tests/fixtures/official_fmesh_case1~5.i，官方 FMESH_INC Inputs 原样复制），**全量 pytest 454/0**（449 基线零回归 + 5 新增）。INP e2e 冒烟：5 官方 case imesh=51/jmesh=10/kmesh=110.0 全非空、out=jk、case2~5 `inc=` 段 raw 保留、R1 不动点成立。改动记 docs/backend-changes.md §M。前端同契约同步。未 commit。
>
> **✅ FMESH 关键字等号可选解析修复（frontend，2026-08-15，feat/meshtal-volume）**：MCNP 允许空格分隔 `imesh 51`、等号可选；原 `_KEY_RE=/^([A-Za-z]+)=(.*)$/` 强制 `=` 导致裸 `imesh` 被当未知 token 跳过、网格字段空。修复：`KEY_RE` 改 `^([A-Za-z]+)(?:=(.*))?$`（等号可选）+ 新增 `KEY_EQ_RE`（未知 key 带 `=` 边界）+ `isKnownKeyToken`（已知关键字边界判定，大小写不敏感）；收集循环 break 条件由「任意字母词」改为「卡族头 / 已知关键字 / 未知 key 带 =」，防 `geom xyz` 的字母值 `xyz` 被误断截断；`inc=` 等未知 key 跳过不报错。JSON key 与后端 fmesh_parser 同步（imesh/iints/... 不变）。测试 +7（空格/等号/混排/inc= 容错/大小写/geom xyz 不截断/round-trip），**vitest 197/0**（190 基线不破）+ tsc EXIT 0 + build EXIT 0。改动记 docs/frontend-changes.md。未 commit。
>
> ⛔ **上级紧急指令（2026-08-14）：全面开源替代审计，审计通过前不复工**。暂停所有在途施工（网格可视化后端 2 项修复、tester 复审、其它在途开发全部待命，工作现场保留不丢）。审计范围扩大为**全面开源替代审计**：① 本次网格体积可视化（体积渲染库 / MESHTAL 解析 / 配色方案 / FMESH 可视化工具）；② 现有核心功能逐项（3D 预览 / 解析器 / 生成器 / 输出分析 / 图表 / 截面 / 材料库 / 文本互转 / 窗口壳）；③ 三层依赖（Python/前端/Rust）。产出"替换建议清单"（现状实现 + 候选开源项目/库 + 好在哪 + 引入成本/风险 + 建议 替换/保持），供上级批准。**只分析不安装、不替换，等上级批准；替换完成后才恢复网格可视化施工**。已派 arch-dep-audit（范围已扩大并入此全面审计）。
>
> **✅ FMESH 卡 FACTOR 乘法因子字段后端支持（backend，2026-08-14，feat/meshtal-volume）**：PM 派发，`FmeshDefinition` 新增 `factor: str = ""`（models.py）；fmesh_parser `_KEYS` 加 `"FACTOR": "factor"` + `_has_structured`/`_card_lines`（非空才发 `FACTOR=`，OUT 后 AXS 前）/CMESH(cyl) 结构化清空循环同步；api_server `_fmesh_from_list` 读 `factor`（缺 key 容忍，出向 asdict 自动带出）。**测试先行**：tests/parser/test_fmesh_parser.py +4（`FACTOR=` 解析→factor / 生成输出 `FACTOR=` / round-trip 保留 / 无 FACTOR 默认空串 / AXS+VEC+TR+OUT+FACTOR 混排不丢），**全量 pytest 449/0**（445 基线零回归 + 4 新增）。冒烟：api 序列化 factor 带出、INP e2e（parse→generate→reparse）FACTOR 保留、CMESH cyl 含 FACTOR 走 raw 降级。契约：字段名 `factor` / 关键字 `FACTOR=`（C810/MCNP6 均有，默认 1，正整数前端表单校验，后端只存原文）。改动记 docs/backend-changes.md §L。前端同步按同一契约。
>
> **✅ UI 隐藏 TMESH 计数卡入口（frontend，2026-08-14，feat/meshtal-volume）**：用户决定 TMESH 以后再更新加入 → **只隐藏"创建/选择 TMESH"入口，不删 TMESH 代码路径**。改动：① FMeshForm.tsx 标题 `网格计数（FMESH/TMESH）`→`网格计数（FMESH）`（:131）+ 文件头注释同步；空态文案去 TMESH（:138）；**kind 下拉移除 TMESH option**（:160）；`tmesh` 字段 label `"TMESH"`→`"时间分箱（TMESH 关键字）"` + hint「不是 TMESH 计数卡」（:31），`t_ints` label→`"TINTS（时间区间数）"`（:32），字段/placeholder/值/后端 key 不变，时间轴动画不受影响。② **导入 TMESH 行 = 只读徽标方案**：新增导出 `fmeshKindControl(kind, geom)`（:44-57）——FMESH→select、TMESH→只读徽标+说明（cyl→「不进入体积可视化」，xyz→「数据原样保留」），行仍在 deck.tally.fmesh 受控列表、round-trip 保真零丢失；选 a 不选 b（raw 兜底其他卡）因 b 破坏受控表单契约+丢数据面。③ **fmeshState.ts 仅改注释/标注未动逻辑**（FmeshKind/FAMILY_RE/TMESH/RMESH/CMESH 解析/emit/序列化全保留，未来启用直接放开 UI）。测试：新增 `gui/test/volume/fmeshKind.test.ts`（3 用例）+ fmeshState.test.ts +1（fmeshDefsToRows kind=TMESH 保留 + round-trip）；无既有测试断言 UI 下拉。**vitest 118 全绿（114 基线不破）+ tsc/build EXIT 0**。改动记 docs/frontend-changes.md。未 commit。
>
> **✅ 材料编辑对话框两 Bug 修复（frontend，2026-08-14，experiment/geouned）**：Bug 1（导入 INP 不自动查截面库）真实根因 = **载入时不自动校验**（手动行 onChange 本就接 validate-zaid，用户描述"没接"不准确；缺挂载 effect，初始圆点恒灰）；修 = 新增 `validateZaid(i,zaid)` 共用 helper + `[initial]` 挂载 effect 自动逐个查库。Bug 2（Fe57 编辑异常）真实根因 = **数值 ZAID（26057）上 `nu.zaid.split("-")` 把整串当元素、mass 段丢失**，value 派生+onChange 回写闭环在同一 nu.zaid 上互错位（改 57 变 2605 / 删 Fe 删掉 57）；修 = 抽导出纯函数 `splitZaid`/`buildZaid`/`resolveZaid`（兼容数值/自然元素/手写横线）+ 每行本地编辑草稿 `rowEdits`（失焦/保存才 buildZaid 回写+校验，参考 TallyTab idsRef 先例）。改动：`gui/src/components/MaterialEditDialog.tsx` + 新增 `gui/test/zaidSplit.test.ts`（22 用例：拆组/组装/往返恒等/Bug2 回归）。**vitest 76 不破（98 全绿）+ tsc/build EXIT 0**。改动记 docs/frontend-changes.md。未 commit。
>
> **✅ 全面开源替代审计完成（arch-dep-audit，2026-08-14，零安装零改动）**：三层依赖 + 网格可视化 + 现有核心功能逐项。**核心结论：自研实现全部站得住，无值得替换项**；唯一 P0 缺口 = 打包 spec 漏新模块。**⚠️ P0 打包缺口（PM 已亲自核实 spec:25 `_keep_dirs=["generator","docs"]` 无 `meshtal`、`_keep_py` 无 app/meshtal）**：`app/meshtal/` 8 模块不落盘 → worker 以磁盘路径启动，**打包版 meshtal-parse/texture 必挂**（v1.7.0 打包前置，非安装类直接排期）。修复：spec `_keep_dirs` 补 `"meshtal"` + worker 路径改 `sys._MEIPASS` 感知（照 step_importer_geouned.py:22）+ 打包冒烟 3 端点。**批准删除（零风险）**：requirements 删 pyvista（0 import）、markdown（Python 包 0 引用）；package.json 删 electron 备用壳（electron:build 调 electron-builder 未声明=坏脚本）+ main.cjs/preload.cjs；Cargo 删 dialog-save/dialog-open + tauri.conf dialog 块（JS 0 引用）。**批准补声明**：requirements 显式补 numpy（现经 pymcnp/matplotlib 传递）。**候选引入（等批准，非本轮）**：输出图表落地时 ECharts/Chart.js；three 官方 Volume 示例（零新依赖）可选对照。**保持现状（关键项）**：matplotlib（pymcnp 硬传递依赖，`import pymcnp` 即触发，删则崩）、FreeCAD（仍最优 CSG）、pymcnp+自研解析/生成（R1 不动点+C810 对齐）、numpy 截面、three/marked/react/@tauri-apps/api、Tauri 主壳。**不引入**：vtk.js/VolView/itk.js/vologram/scijs（体积渲染架构冲突/无收益）、mcnptools（MESHTAL 覆盖无优势+pymcnp 生态绑定）、matplotlib colormap/chroma.js（golden 双端契约重锁成本>收益）、CadQuery/OpenSCAD、trimesh/numpy-stl。**状态：上级已批准（2026-08-14）→ 清理已执行零回归 → 网格可视化施工已恢复**。
>
> **🧹 依赖清理完成（cleanup-deps，2026-08-14，上级批准项全部执行，零回归确认）**：① requirements.txt 删 pyvista/markdown + 显式补 numpy（matplotlib 保留=硬依赖）；② package.json 删 electron devDep + electron:dev/electron:build 脚本 + main 字段；③ `gui/electron/` 整目录删（main.cjs/preload.cjs git rm）；④ Cargo.toml tauri features 删 dialog-open/dialog-save；⑤ tauri.conf.json allowlist 删 dialog 块（联动一致）；⑥ package-lock.json 手动剔除 electron 条目（保留 electron-to-chromium=browserslist 依赖，不跑 npm install）。**零回归**：pytest 428/2 → 428/2（唯二红=网格可视化施工待收尾项，非回归）、vitest 76 → 76。**PM 三项裁决**：① 删 `gui/test_electron.cjs`（require electron 残留，属批准删除范围）；② 删 `gui/backend/api_server.py:10-11` PYVISTA_OFF_SCREEN 死配置（pyvista 0 import）；③ `gui/test/volume/colorize.test.ts:93` 128³ 计时 <50ms 为 **flaky**（负载高偶发 55.38ms，非清理回归），已记录，待测试稳健化。**未 commit，提交时机由 PM 定**。
>
> **🔌 网格可视化后端施工断点（backend-meshtal 已停工确认，2026-08-14）**：复工断点如下——**已完成并验证**：`_mode_parse` manifest 解析元数据缓存（valid_39 二次 parse 3.07s→0.009s，<1s 达成）+ `meshtal_cache` 增 get_manifest/put_manifest；已写 `tests/unit/test_meshtal_cache.py`（10 用例绿）+ `tests/unit/test_meshtal_worker.py` + `tests/parser/test_fmesh_parser.py`（后两者各 1 用例待修）。**未完成**：① worker `_run` 未包 error 信封（texture 坏 tally 直抛 KeyError，try/except 需从 main 移到 _run）；② fmesh_parser 多区间 IINTS=`10 20 IINTS=2 2` 解析出 `iints='2'` 非 `'2 2'`（bug 未定位）。已知待修测试 2 条：`test_worker_texture_bad_tally_errors` / `test_multi_interval_imesh_preserved`（2 failed / 19 passed in 3 新文件）。**复工通知：上级批准替换审计清单后。**
>
> **✅ 网格可视化后端复工收尾完成（backend-meshtal，2026-08-14）**：① worker `_run` 包 error 信封（try/except 从 main 移到 `_run`，`test_worker_texture_bad_tally_errors` 绿）；② fmesh 多区间 IINTS —— **非解析器 bug，是测试卡体笔误**（测试第二行误写 `JMESH=10 IINTS=2` 重复 IINTS 覆盖首行，修正为 `JMESH=10 20 JINTS=2 2`，`test_multi_interval_imesh_preserved` 绿）；③ **P0 spec 补丁完成**：spec `_keep_dirs` 加 `"meshtal"` + api_server 新增 `_meshtal_worker_script()`（`sys._MEIPASS` 感知，照 step_importer_geouned.py:22，两处 handler 改调该 helper）；④ 删 api_server PYVISTA_OFF_SCREEN 死配置 + `_pv_os` alias。**全量 pytest 430/0**（343 基线零回归 + 3 新测试文件 21 用例全绿 + meshtal 新增全绿）、R1 不回归、vitest 76 不破（后端未碰前端）。**⛔ `gui/test_electron.cjs` 删除已由用户明确授权并执行**（tester-meshtal-final 复审时 `git rm` 完成）。
>
> **✅ 网格计数可视化 · 全链路交付完成（2026-08-14）**：**最终复审通过**（tester-meshtal-final，致命 0 / 严重 0 / 建议 1）：① test_electron.cjs 已删；② 2 项严重闭环——parse 元数据缓存实测二次 HTTP **0.233s <1s**（valid_39 3.36MB，冷 3.308s）、§9.1 三测试文件（cache 9 + worker 5 + fmesh_parser 7）全绿；③ P0 spec 补丁生效（`_keep_dirs=["generator","docs","meshtal"]` + `_meshtal_worker_script()` MEIPASS 感知 api_server:34-42，parse:809/texture:880 调用）；④ 全绿 pytest **430/0** + vitest **76/13** + tsc/build EXIT 0 + 漂移闸门 25→28 双向 + R1 不回归；⑤ KPI 实测 preview HIT 0.011s / texture 0.402-0.582s / parse 缓存 0.233s / 并发探活 3.6-17.2ms / 128³ 切帧 <50ms；⑥ F1-F5 全过；⑦ 3 端点 HTTP 冒烟通过（detect 扫到 / parse grid_bounds+match / texture 标量帧 base64 往返）。**批量提交完成**：`ea20ad7 feat(meshtal): FMESH/TMESH 3D volume visualization + deps cleanup` @ 分支 `feat/meshtal-volume`（基线 experiment/geouned@29d9d93），73 files +68088/-114（大插入=valid_38/39.meshtal fixtures 真实输出约 6 万行），工作区干净，未 push。**打包就绪（v1.7.0 前置）**：spec 含 meshtal + 3 端点冒烟通过；⚠️ 唯一待办 = `#/volume` 真浏览器冒烟待人工补跑（不阻塞），打包 v1.7.0 前补即可。**（此"打包就绪"表述已被 v1.7.0 实包冒烟证伪，见下）**
>
> **⛔ v1.7.0 实包冒烟失败（packager-v170，2026-08-14）——P0 打包模式 meshtal worker spawn 失效**：vite/PyInstaller/tauri build/部署全成功（`_internal/app/meshtal/` 8 模块+__init__ 落盘确认、preview_cache/geouned 在位、**6.2 时效校验命中增量坑已手动覆盖**新 sidecar 25056527B/13:13、版本 1.7.0 双信号 exe FileVersion+bundle grep 通过、5001 探活+meshtal-detect 过）。**但 meshtal-parse/texture HTTP 挂起 >120s**。根因（三重实证）：`api_server.py:811/:882` 用 `subprocess.run([sys.executable, worker])` spawn worker，打包版 `sys.executable`=冻结 PyInstaller sidecar exe → 带任何参数都运行冻结入口 mcnp_bridge 再启第二 5001 服务器（netstat 实证 LISTENING）→ 端口冲突挂起；dev 模式真实解释器跑 worker 完全正常（grid_bounds 正确、tally=4）。**此前的 spec 落盘修复（_keep_dirs 加 meshtal + _meshtal_worker_script MEIPASS）只解决 worker 脚本落盘，未解决"打包版如何 spawn 子进程"**（冻结 exe 不能当解释器跑脚本；freecad/geouned worker 用 FreeCAD 真 python 不受影响）。**修复方向**：mcnp_bridge.py 加 argv 分派（`--meshtal-worker` 调 `_meshtal_worker.main()`）+ api_server 两处 spawn 改 frozen 分支 `[sys.executable, "--meshtal-worker"]` / dev 分支传 mcnp_bridge 路径 + spec hiddenimports 补 meshtal 模块进 PYZ（落盘 data 不可 PYZ 导入）。**未 commit**（冒烟未过；版本四处+README 已在工作树 1.7.0）。已派后端修复 → 重新打包 → 重新冒烟。
>
> **✅ 前端 UI 调整两处（frontend，2026-08-14，experiment/geouned）**：① TallyTab.tsx 删除 F1-F8 可展开参考表（`<details>` 整块）；② 3D 结果入口统一到「输出」标签页——新建共享模块 `gui/src/volume/openVolume3DWindow.ts`（`openVolume3DWindow(path/tally/resolution/model/parseResult)` 封装原 FMeshForm.openWindow 数据桥+非 Tauri fallback，行为逐字节一致），FMeshForm 退化为纯结构化表单（移除启动器与启动按钮），OutputTab 新增「网格计数 3D 结果」小节（解析 MESHTAL 自动探测 / 选择 meshtal 文件 / 3D 体积可视化 + tally 选择 + F3 预算弹窗）。**零测试改动**：vitest 76 全绿 + tsc EXIT 0 + build EXIT 0。改动记 docs/frontend-changes.md。未 commit。
>
> **✅ v1.7.0 已打包部署交付（packager-v170，2026-08-14）——P0 修复在打包版验证通过**：全流程重跑（vite 2.99s → PyInstaller 重打 sidecar 25084986B（+28KB=hiddenimports 9 模块进 PYZ）→ binaries → tauri build 19.5s → **6.2 时效校验再次命中增量坑已手动覆盖**（target/release 旧 25056527B/13:13 → 新 25084986B/13:42）→ 部署 → 冒烟）。**P0 两端点实测不再挂起**：meshtal-parse 冷 3.300s / 二次缓存 0.419s（grid_bounds min[-100,-100,-150] max[100,100,-50]、tally=4/geom=xyz）、meshtal-texture 0.443s（标量帧 Uint8 往返、worldBox 正确）、detect 0.063s；5001 探活 xsdir loaded:true；**版本 1.7.0 双信号**（exe FileVersion + bundle 含 1.7.0）。**提交 `fcc935b release(v1.7.0): FMESH/TMESH mesh tally volume visualization`** @ 分支 feat/meshtal-volume（11 files +145/-118，未 push，工作区干净）。**部署** D:\MCNP\MCNP输入卡生成器：exe 6377984B / python.exe 25084986B / _internal 完整（preview_cache.py 7481B ✓ / app/meshtal 9 项 ✓ / vendor/geouned ✓）。**遗留**：`#/volume` 真浏览器人工冒烟待补跑（不阻塞交付，用户有空自测）；detect 按设计只扫 meshtal/MSHT 开头文件名（符合设计）。环境已清理（无残留进程、5001 空闲）。
>
> **✅ 份额语义 + 计数类型 + 材料编辑修复批已交付并重打包（2026-08-14）**：上级三项决定执行完毕。① 比例归一化**保留现状** + 前端提示（MaterialEditDialog :340-343「份额已归一化（总和=1）；化学式:比例 只影响各成分的相对比例，结果仍会归一化」，后端归一化 api_server.py:636-638 未动）；② 计数类型自动变修复（TallyTab `parseTallyTypeNumber` :45-57 剥前导 F + 剥 X/Y/Z 后缀 → 个位数映射类型，`handleNumberChange` :150-159 接线，`parseF5Variant` :62-70 委托通用解析，新增 `gui/test/tallyTypeNumber.test.ts` 10 用例覆盖 25/F25/25X/F25X/5/1/12/24/6/17/8/F4/无效兜底）；③ `/api/expand-formula` 加 `is_weight` 参数（api_server :606-651，缺省 true=质量份额负号 / false=原子份额正号，防御性布尔解析）+ api.yaml 文档化 + `tests/integration/test_api_expand_formula.py` 7 用例（默认向后兼容逐字节一致）。份额标注/切换（前端）+ 材料编辑双 bug 修复（载入自动校验 + 稳定编辑 splitZaid/buildZaid）+ 前端 UI 调整（删 F1-F8 参考表 + 3D 结果入口移输出页 + openVolume3DWindow 共享模块）全部落地。**基线**：vitest **114**（76 基线 + 各批新增）、pytest **440**、tsc/build 过。**提交 `b98502c feat(ui): material fraction semantics + tally type auto-change + material edit fixes + volume 3D entry to OutputTab`** @ feat/meshtal-volume（14 files +718/-350，未 push）。**重打包 v1.7.0 完成冒烟全绿**：6.2 时效校验第三次命中增量坑已手动覆盖（25085148B/14:54）；5001 探活 + 3 meshtal 端点不挂起（detect 0.062s / parse 3.367s / texture 0.416s）+ **expand-formula 两态 is_weight 冒烟**（默认负=质量、is_weight:false 正=原子互为相反数）+ 版本 1.7.0 双信号（exe FileVersion + bundle）；部署 D:\MCNP\MCNP输入卡生成器（exe 6379008B / python.exe 25085148B / _internal 完整：preview_cache + app/meshtal 9 项 + geouned）。环境已清理（无残留进程、5001 空闲）。
>
> **✅ FMESH 表单改进（对照 C810/MCNP6）前后端完成（2026-08-14）**：字段对齐 MCNP6（`eints→emints`、`t_ints→tmints`，解析容错 EINTS/EMINTS、TINTS/TMINTS 双拼写）；新增 `axs`/`vec`（cyl 系显示 + 不平行校验）/`tr`（可选变换）；`out` 确认支持；GEOM 连写单 token（`GEOM=XYZ`，防 `GEOM=X Y Z` 非法）；OUT 九选下拉（COL/CF/COLSC/CFSC/IJ/IK/JK/NONE/XDMF + CF 体积/NONE 不打印 meshtal/XDMF 供 ParaView 说明）；MAT 帮助文案（0=粒子所在格材料/非 0=指定）；校验规则纯函数 `validateFmeshRow`（正整数/条目数匹配/单调递增/cyl kmesh 末值=1/向量平行/TR/大网格内存警告阈值 128³ 按三方向**区间总数**乘积）。**后端 pytest 445/0**（440 基线 + 5 新增；含 `_is_fmesh_body_line` 正则收紧负向前瞻修 `TMESH=` 时间关键字被误判为 TMESH 卡头既有缺陷）；**前端 vitest 145**（118 基线 + 27 新增；`gui/test/volume/fmeshValidation.test.ts` 24 用例）。前后端三处逐字一致（生成关键字/解析容错/载荷 JSON key），无仲裁项；唯一语义差异=前端 geom 归一化大写单 token（MCNP 大小写不敏感，round-trip 稳定）。**未 commit，等上级定提交/重打包。**
>
> **✅ FMESH 傻瓜友好改造 + factor 全批交付并重打包 v1.7.0（2026-08-14，fast-finish-v170）**：pytest **449/0**（445 基线 + factor 4 条）、vitest **190/0**（155 基线 + 35 新增：fmeshFactor 9 / simpleMode 4 / surfacesAABB 22）、tsc/build 过。傻瓜改造：**简单/高级模式**（默认简单=粒子+IMESH/JMESH/KMESH，高级展开 ORIGIN/INTS/能量时间/MAT/OUT/AXS/VEC/TR/factor）+ **⚡按几何自动填充**（`gui/src/volume/surfacesAABB.ts` `computeSurfacesAABB` 解析平面/球/圆柱/宏体 + TR 平移旋转容错，解不出→null 提示，`aabbToFmeshValues` 填 ORIGIN/IMESH/JMESH/KMESH）+ 粒子说明（每个网格计数一个粒子 N/P/E）+ 三步引导（① 选粒子 ② 点自动填充 ③ 解析看 3D 结果）；**factor 字段**（`FACTOR=` 关键字，默认 1，正整数校验，前后端契约一致字段名 factor）。**提交 `0457871 feat(fmesh): FMESH form fool-proof mode + geometry auto-fill + factor + MCNP6 keywords + hide TMESH UI`** @ feat/meshtal-volume（19 files +1955/-48，未 push）。**重打包 v1.7.0 冒烟全绿**：6.2 时效第四次命中增量坑已手动覆盖（25086235B/20:28）；5001 loaded:true；3 meshtal 端点不挂起（detect / parse 3.305s / texture 0.422s）；新关键字 parse/generate 往返（fmesh_defs 含 emints/tmints/factor/axs/vec/out + 卡体 `EMINTS=`/`TMINTS=`/`FACTOR=2`/`GEOM=XYZ` 连写）；版本 1.7.0 双信号；部署 D:\MCNP\MCNP输入卡生成器（exe 6389760B / python.exe 25086235B / _internal 完整）。环境已清理。遗留：#/volume 真浏览器人工冒烟待补跑（非阻塞）。
>
> **🔧 关键字解析修复 + FMeshForm 9 行布局批交付（2026-08-14）**：官方 5 个 FMESH 测试文件暴露：`_KEY_RE` 强制 `=`，而 MCNP 允许空格分隔（`imesh 51`）→ 导入网格字段空。修复（前后端一致，JSON key 零改动）：`_KEY_RE` 等号可选（fmesh_parser.py:20 / fmeshState.ts:199）+ 新增 `_is_boundary_token`/`isKnownKeyToken`（收集循环 break 改"卡族头/已知关键字/未知 key 带 ="，防 `geom xyz` 字母值被误断、防 `inc=` 被 out 吞并）+ 未知关键字（`inc=`）跳过不报错 + 整段记 raw 兜底回放（round-trip 不丢）。vendor 官方 `tests/fixtures/official_fmesh_case1~5.i`。**验证**：pytest **454/0**（449 基线 + 5，官方 case 全解析 imesh=51/jmesh=10/kmesh=110.0 非空、out=jk、inc= raw 保留、R1 不动点）、vitest **197/197**（197 基线：-4 simpleMode +4 fmeshLayout）、tsc/build 过。**布局（用户真实需求=控件分行，非改预览格式）**：FMeshForm 去简单/高级切换（始终完整表单）+ **9 行分组**（行1 卡头粒子|GEOM|OUT / 行2 ORIGIN / 行3 IMESH|IINTS / 行4 JMESH|JINTS / 行5 KMESH|KINTS / 行6 EMESH|EMINTS / 行7 TMESH|TMINTS / 行8 MAT|FACTOR|TR / 行9 AXS|VEC 仅圆柱），**预览/生成格式零改动**（fmeshToCardText/cardLines 未动），保留 ⚡自动填充/粒子说明/三步引导/factor。**提交 `f39a218 fix(fmesh): optional-= keyword parsing (official case1-5) + FMeshForm row layout`** @ feat/meshtal-volume（15 files +719/-104，未 push，工作区干净）。**建议：重打包 v1.7.0 把此修复带进已部署版**（用户实测官方测试文件命中该 bug，当前部署版有缺陷）。
>
> **🔄 依赖红线政策更新（2026-08-14 上级）**：原"零新依赖、禁止安装任何东西"调整为 **"先提出、批准后安装"**——默认仍优先零依赖（现有 stdlib/numpy/three/pymcnp 够用就不装）；但有明显更好的库（体积渲染 / MESHTAL 解析 / 性能优化等场景）**团队可提出建议**（库名 / 解决什么 / 比现有好在哪 / 体积与风险），**上级批准后才安装**；未经批准一律不装（红线仍有效，只是从"一律不装"改为"批准才装"）。**依赖审计任务已派**（arch-dep-audit：三层依赖盘点 + 开源替代建议，只分析不安装）。
>
> **✅ 网格计数可视化·独立复核（tester-meshtal-accept，2026-08-14）：有条件通过**。全量 pytest **409/0**（343 基线 + 66 新增零回归）、vitest **76**（19 基线 + 57 新增）、tsc/build EXIT 0、漂移闸门 25→28 双向绿、R1 不动点绿、§7.3 对齐断言 5 用例绿、KPI 核心达标（preview HIT 0.041s / texture 0.202s / 128³ 切帧 <50ms / worker 并发探活 2-25ms 不卡 5001）、F1-F5 全过、纪律零违例。**⚠️ 严重 2 项（后端补齐后转通过）**：① `_meshtal_worker.py` `_mode_parse` 只 cache.put 从不 cache.get → 重复 parse 全量重解析 3.07s，契约 §4.4/§8 "parse 元数据缓存命中 <1s" 未达成；② 契约 §9.1 三测试文件缺失（test_meshtal_cache.py / test_meshtal_worker.py / test_fmesh_parser.py）。💡 `#/volume` 真浏览器冒烟待人工补跑。已派 backend-meshtal 修复两处 + tester 复审。

> **🚀 下次打包版本号 = 1.7.0（2026-08-14 用户明确指定，非 1.6.5）**：网格计数可视化（FMESH/TMESH 3D 体积渲染）是下个大功能，按 **1.7.0** 发布。当前已交付 **1.6.4**（commit 29d9d93）。打包时（手动打包方法步骤 1）版本号要改**三处 + README 徽章**：`gui\src-tauri\tauri.conf.json`（package.version）/ `gui\package.json`（version）/ `gui\src-tauri\Cargo.toml`（version，**v1.6.4 曾漏改教训**，当前停在 1.6.3）/ `README.md` 徽章（:9）。已同步更新 `docs/手动打包方法.md` 步骤 1（两处→三处+README，含 Cargo.toml 漏改失败案例）+ 踩坑清单 #6 + 速查 + 与 release.bat 对照表；契约 `docs/contracts/meshtal-visualization.md` 已标注交付版本目标 1.7.0。
>
> **⚠️ Cargo.toml 版本隐患（2026-08-14 上级核实）**：`gui\src-tauri\Cargo.toml` version 停在 **1.6.3**，v1.6.4 打包时漏改（手动打包文档当时只写"tauri.conf.json + package.json 两处"）。Tauri 以 tauri.conf.json 为权威、不影响打包，但版本号不一致是隐患 → 手动打包方法步骤 1 已改为"三处+README"，Cargo.toml 列入必改清单。
>
> **✅ 网格计数可视化契约已锁定（2026-08-14，architect-meshtal）**：`docs/contracts/meshtal-visualization.md`（16 节，对齐 preview3d-performance/geometry-check 风格）。用户已敲定全部澄清项：**FMESH+TMESH 都进 v1**、几何外壳**像 3D 预览可交互**（勾选栅元+粒子，复用 Preview3D 勾选逻辑）、**box-average 均值降采样**（保总量）、128³ 默认 / GPU 驻留 ≤256MB / 256³ 显式 / 超预算弹窗"流畅/精细"。模块化新建 `app/meshtal/`（meshtal_parser/volume_builder/colormap/downsample_plan/meshtal_cache/_meshtal_worker/fmesh_parser）+ `gui/src/volume/`（volumeShader/VolumeRenderer/colorize/alignWorld/downsampleRequest/fmeshState/FMeshForm/VolumeControlPanel/ResultWindow）+ 3 端点（meshtal-detect/parse/texture，25→28 漂移闸门双向）+ 独立窗口 `volume3d`（windows.ts 桥 + main.rs command）+ `#/volume` 路由。**关键裁决**：meshtal-texture 返回**标量帧 Uint8**（前端 colorize 上色，改上下限不重取纹理）、CPU 端上色避开 float 纹理扩展坑、几何外壳与体积盒**统一归一化共享 offset 对齐**（契约 §7.3 四断言）、大文件解析走子进程 worker + pickle 缓存、**不改 Preview3D.tsx 主组件**、体积窗口关闭不清 STL 会话、TMESH cyl（CMESH）解析但 unsupported_geom 降级、配色 python/TS 双端 golden sha256 对照防漂移。交付版本目标 **1.7.0**。测试先行：tester 先建红基线（fmesh 导入往返 + R1 不动点 + vendor valid_38/39/40 + 闸门预置 3 operationId 红）→ 后端五步走（sections 补 `^TMESH` + core 入口门 → fmesh_parser → models 字段 → generator 回放 → api_server 三处序列化）→ app/meshtal/ 六模块 → 3 端点 + api.yaml → 前端 volume 9 模块 + 窗口 → tester 复核（对齐断言/KPI/e2e）。**施工已启动（2026-08-14，见下）。**
>
> **🔨 网格计数可视化·后端施工完成（2026-08-14，backend-meshtal）**：红基线 63 红 → **62 绿 + 1 待仲裁**；全量 pytest **408 通过 / 1 失败**（343 基线零回归 + 65 新增绿）。FMESH/TMESH 五步走完成（sections 补 `^TMESH`/`RMESHn`/`CMESHn` + core.py 入口门吸收 fmesh_defs + app/meshtal/fmesh_parser.py + models.FmeshDefinition + generator 回放 + api 序列化）；`app/meshtal/` 8 模块完成（meshtal_parser/volume_builder/colormap/downsample_plan/meshtal_cache/deck_match/_meshtal_worker）；3 端点（meshtal-detect/parse/texture）+ api.yaml 25→28 漂移闸门双向绿；`_err` 增可选 hint 加性兼容（既有端点错误响应无 hint 字段）。**伴生修复**：core.py `pending_c` C 注释 EOF 静默丢弃 → flush 进 other_cards（R1 必要，343 基线零回归证明安全）。**1 红待仲裁**：test_meshtal_colormap.py `test_weather_lut_midpoints` 断言 `lut[191]=(249,115,22)` 与 golden `36770ae2…`（t-space 插值锁定 `(249,116,22)`）互斥。
>
> **⚖️ PM 仲裁（2026-08-14）**：**golden sha256 为准**（§4.3.1 跨语言防漂移强契约，tester 按 t-space 插值实算、后端精确复现）；midpoints `(249,115,22)` 系目测锚点值未按插值实算 → **修正为 `(249,116,22)`**（补正红基线测试内部自相矛盾，非删弱既有断言，正当地下不违反"禁改断言"纪律）。契约 §3.4 MESHTAL 行序 `X Y Z E T Result` 与实际 MCNP `Energy Time X Y Z …` 不符（vendor 实核），已请架构师勘误。

> **🛑 决策（2026-08-13 上级）：停止 release.bat 自动化发布，改为手动打包方法文档**。原因：release.bat 自检环节后端拉不起 5001（未解决）；自动化脚本在 Git Bash 环境下有 MSYS 路径转换坑（`cmd /c` 被吞 `/c`、`cmd //c` 报"找不到 // 文件"）。**动作**：① 立即终止 releaser-v164 运行 + 杀残留进程（MCNP exe / sidecar python）+ 确认 5001 空闲；② 保留已提交成果（版本 1.6.4 commit **29d9d93**、build.bat 已删、本批改动）；③ 写手动打包方法文档 `docs/手动打包方法.md`（基于两次成功手动打包：vite → PyInstaller → 替换 binaries → tauri build → 复制 onedir → 冒烟），必须含踩坑：Git Bash 跑 .bat 被 MSYS 吞 `/c`/`//`（用 cmd 窗口直接跑或 `MSYS_NO_PATHCONV=1`）、`bundle.active=false` → 产物在 target/release 从那里复制 onedir、sidecar=python.exe+_internal 完整复制、spec 必须含 preview_cache.py 否则启动即崩、冒烟=起 exe→5001 探活→3D 出图、版本提升位置=tauri.conf.json+package.json。**release.bat 自检 bug 未定位，搁置**；后续出包一律手动或修好自检后再议。
>
> **✅ 手动打包方法文档已完成（doc-pack-manual，2026-08-13）**：`docs/手动打包方法.md`（9 章 + 附录：〇总览/1 版本提升/2 门禁/3 vite/4 PyInstaller+spec 核对/5 sidecar 替换 binaries/6 tauri build+**6.2 sidecar 时效校验**/7 部署核对/8 冒烟/9 踩坑 7 条+速查+与 release.bat 对比）。**踩坑 7 条全覆盖**：MSYS 吞 `/c`/`//`、bundle.active=false 产物在 target/release、sidecar onedir 完整复制、spec 必含 preview_cache.py、冒烟三步、版本两处改、**tauri build 增量编译不刷新 target/release 的 sidecar（mtime 陈旧→版本号新后端旧）需手动覆盖**。**技术确认**：mcnp_bridge.py/api_server.py 编译进 PYZ archive 不落盘（核对真实产物 preview_cache.py+vendor/geouned，靠冒烟验证）；门禁基线用 343 最新值。**发布失败根因诊断（releaser-v164）**：门禁+构建全绿，但 tauri build 后 `target/release/python.exe`（08-04 陈旧）与 `_internal`（08-12 陈旧）未刷新 → 部署的后端是 v1.6.3 旧 sidecar；自检 5001 失败 = Git Bash GNU timeout 遮蔽 Windows timeout（90 次重试几秒耗尽）+ 部署后端旧。均非业务代码缺陷。

> **🚀 当前执行计划：发布 v1.6.4（2026-08-13 上级拍板）**：①版本 1.6.3→1.6.4（tauri.conf.json + package.json + README）；②提交本批次全部改动（7 反馈 + 词条专项 D-01~D-13 代码/文档/测试 + release.bat + README-release.md + 契约文档）并删除过时 build.bat；③关闭 PID 15548（打包版残留后端占 5001）；④release.bat 一键发布（门禁 pytest 343 + vitest 19 → 构建 → 部署 D:\MCNP\MCNP输入卡生成器 → 自检）；⑤tester 验证产物（exe 启动 + 5001 探活 + 3D 出图 + 版本 1.6.4）。**步 1-4 已完成（releaser-v164，2026-08-13）**：版本已提升 1.6.4 并 commit（**commit 29d9d93** `release(v1.6.4): user feedback batch + lexicon audit + one-click release script`）、build.bat 已删、PID 15548 已自动退出且 5001 空闲。**⚠️ 踩坑：Git Bash 下 `cmd /c release.bat` 的 MSYS 路径转换会吞掉 `/c` → cmd 进交互模式、stdin EOF 立即退出（exit 0），脚本从未运行**（"10+ 分钟未完成"实为脚本未启动，非卡点；此前 v1.6.3 遗留产物误导判断）。**修法：用 `cmd //c`（双斜杠转义）重跑，stdin 重定向 /dev/null 防 pause 阻塞**。tauri.conf.json `bundle.active:false` → bundle 目录为空是正常配置（release.bat 直接复制 target/release onedir，不依赖 bundle）。**重跑进行中（预计 5-10 分钟）**。**红线**：不自动安装依赖；release.bat 门禁 npx vitest/npm run build 不得触发依赖获取；破坏性操作被权限拦截 report 别强绕。#7（3D 重合检查施工）今晚不启动等用户另行指示。


> **✅ 批次终态（2026-08-13）**：**用户 7 条反馈 + 词条专项全部完成**。全量 pytest **343 绿 / 0 红**（271 基线 + 10 #5 + 6 #1/#6 回归 + 6 D-01 + 4 #2 + 45 D-03/D-07/D-10 + 1 D-07 对照）+ vitest **19/19**。R1/R2-R4/契约漂移闸门/样例冒烟全绿。词条专项差异表 **13 条定稿**（docs/contracts/card-lexicon.md + docs/card-lexicon-diff.md），权威源=C810.pdf（MCNP5 卷 II Ch.3）+ MCNP6 专属标注待核验。**剩余待办**：D-13 待 MCNP6 手册卷 II Ch.3 核验（本机无）、#7 geometry-check 施工排期（契约已存档待排）、D-02/D-04/D-06 兜底登记可选优化、D-09 兜底可靠性抽测（EMBED/DXCn/STOP）、及此前搁置事项（release.bat 实跑验证/commit 时机、build.bat 是否废弃、5001 端口打包版残留后端进程、Qt 残留清理 git rm gui/build_onedir/ + requirements.txt 去 PyQt5/pyvistaqt）。


> **当前执行计划：用户 7 条反馈工单（2026-08-13 上级转来，来源 P:\dekstop\新建 文本文档 (2).txt）**：P0 严重（#6 cell 文本→表单转换丢数据最严重、#1 FM 卡导入不识别）→ P1 功能/UI（#3 F 卡输入光标消失、#5 材料 nlib/gas/plib 改"其他"框+换行透传、#4 高级其他卡空间窄）→ P2（#2 */+ 前缀是否忽视——初核大概率已支持待补往返测试、#7 3D 预览重合栅元无预警=增强待方案）。**修时以 app/docs 蒸馏知识为准**（#1/#2→MCNP6_FN卡结构参考.md+C810；#6→sample_format+曲面卡格式参考；#7→曲面卡格式参考理解栅元边界语义；#5→sample_format 材料卡格式）。#1 初核根因：parse_data_cards 入口门（parsers/core.py:1001-1003）正则 `^[*+]?F\d+:` 要求 F 后跟数字，`FM5 8.65E10 1 -5 -6` 不匹配→掉 other_cards 计数乘子丢失（参考 photon_skyshine.inp:127）；#2 初核 parse_f_tally 有 fn_prefix 处理（core.py:715-719）。**已完成**：✅ **frontend-fb1 #3/#4/#5 前端落地**（TallyTab.tsx idsRef 按位置缓存 id 行不重挂载 / AdvancedTab.tsx 其他卡 minHeight 100→240 保留 resize / MaterialEditDialog.tsx label 改"其他"+textarea minHeight60 等宽字体；vitest 5 文件 19 用例全绿 + tsc EXIT 0 + build EXIT 0；改动记 docs/frontend-changes.md §反馈）；✅ **arch-fb7 #7 契约存档**（`docs/contracts/geometry-check.md` 已落，方案 A FreeCAD 布尔求交 + AABB 预过滤，独立端点 /api/check-overlap 25→26，severity/容差/上限/模块 seam/验收标准齐，本轮不施工；ADR 已补）。

**已完成**：✅ **backend-fb5 #5 后端生成器联动**（inp_generator.py `_generate_materials` 205-220：options 按 `\n` 拆分，首段内联 M{n} 保 F-D 单行 pin 字节不变，余段 5 空格缩进作 M 卡续行防 gas=/plib= 当新卡；`\r\n` 归一化、空白段跳过；`_wrap_long_lines`（1110）交互验证超长行 & 续行 ≤80 列无二次污染；新增 tests/unit/test_generator_materials_multiline_options.py 10 用例；全量 pytest **287 绿 / 0 红**（基线 277 零回归）+ R1 不动点不回归；解析器 core.py:397 拍平单空格属接受行为未改）。**前端乘子 UI 未开工**（#1 后端 API 结构已定：tallies 每项带 `multiplier` 字符串，零后端改动即可派前端 TallyTab 加乘子框）。

**已完成**：✅ **#1/#6 后端 P0 修复（2026-08-13）**：
- **#1 FM 全链补 multiplier**：入口门扩 `^[*+]?FM\d+`（core.py:1030）/ `parse_f_tally` FM 分支（:721-737，附加同 number 的 TallyDefinition；Fn 未先行建占位 type="" 保证不丢）+ F 卡合并块吸收占位（:790-811）/ `TallyDefinition.multiplier`（models.py:200）/ `_generate_tallies` F 卡后回放 `FM{n}  {multiplier}` + 占位只回放 FM（inp_generator.py:644-671）/ api_server 三处序列化带出（_tally_from_dict:358、_deck_to_frontend_dict:480、_handle_parse_inp:665）/ sections.py DATA_PATTERNS 补 `^FM\d+$`（:146）。
- **#6 cell 文本→表单**：`_handle_text_to_section` cells 分支独立 shell（api_server.py:718-719）——用户 cell 文本入【栅元段】，material/density/surface_expr/imp/vol/tmp/pwt/comment 全保留不再落 other_cards；materials/tally 分支保持原 shell 不变。
- **回归测试状态（重要）**：tester 提交的 2 回归文件 pin 的是修复前 bug 行为（提交时即绿，与"现在红"不符），修复会让 2 个用例变红；已把 6 用例**更新为断言修复后正确行为**（强化非删弱）→ 全绿。全量 pytest **287 绿 / 0 红**（271 基线 + 6 回归 + 10 #5）+ R1/R2-R4/契约漂移闸门全绿。改动清单 docs/backend-changes.md 附录 D。

**在途**：**arch-lexicon 执行"解析器词条与知识库对齐审计"专项**（上级 2026-08-13 提出，比单独修 FM 更重要——FM 暴露系统性缺陷：解析器卡类型清单未与知识库对齐，可能还有别的漏网卡）。专项 = 架构师审计（只读）：从 app/docs/ 提取 MCNP 卡类型权威词条目录（C810 卡索引为主 + FN/曲面/输出/PRINT/源分布/sample_format），映射 parsers/core.py parse_data_cards 入口门 + 各 parse_* 分支成"解析器实际认识清单"，比对产出差异表（FM 已知，重点找 FS/FT/FQ/SD/CF/TF/DE/DF/E4/T4 等计数辅助卡 + 其他卡），产 `docs/contracts/card-lexicon.md`（词条目录=单一事实来源，防漂移）+ 差异表 + 修复建议。系统性修复由后端在 #1/#6 收尾后执行（暂未派）。**#2 */+ 前缀**：✅ 已补往返测试（tester，tests/parser/test_regress_fm_prefix.py 4 用例全绿，2026-08-13）：*F4/+F4/F4 的 fn_prefix 导入→生成回放→完整 INP 往返不丢。纪律：基线 pytest 277→287（+10） + vitest 19 不破。

> **✅ 词条专项重评完成并通过（arch-lexicon，2026-08-13，按设计意图校准）**：差异表 10 条 = **真需修 3**（D-03【P1】sections.py 分节缺口：未设计卡节首误分曲面/栅元段→兜底网失效，补 DATA_PATTERNS；D-07【P2】SDEF 裸参数白名单缺 ERG/WGT/CEL/TME/EFF/X/Y/Z/RAD/EXT/AXS/VEC/DIR 静默丢值，补进 _apply_sdef_param；D-10【P2 新增】other_cards 行内 $ 注释被 strip_comment 剥离→round-trip 丢，append 改存 raw_line，影响全部未设计卡含 D-09）+ **高价值卡结构化 1**（D-05 前端 TallyTab 乘子框，后端已全链修零后端改动）+ **兜底可靠登记 5**（D-02 计数辅助卡 verbatim 不丢 / D-04 known 告警可选优化 / D-06 En 参数化原文保留 / D-08 md 已修 / D-09 PyMCNP ~35 族设计行为非缺陷→降 P2/P3 兜底可靠性抽测 EMBED/DXCn/STOP）+ 已修 D-01。**已派 backend-fb16 执行 D-03/D-07/D-10**（测试先行：tester-dfix 建红基线→backend 修转绿；D-03 最优先，验收基线 pytest 293→295+，R1 漂移闸门不破）。**✅ D-03/D-07/D-10 红基线已就位（tester-dfix，2026-08-13）**：3 新文件——`tests/parser/test_regress_lexicon_d03_sections.py`（29 红：ELPT:N 节首误分 cell_lines / 紧凑 INP *F4 误分 surf_lines / 21 词条参数化）、`test_regress_lexicon_d07_sdef_bare.py`（12 用例 11 红+1 对照绿：SDEF ERG 14→erg='' / AXS 0 0 1→axs='' / POS=0 0 0 ERG 14 WGT 1 续值吞 key / 对照 EFF=0.5 绿不回归）、`test_regress_lexicon_d10_other_cards_comment.py`（5 红：PTRAC $ 注释被 strip_comment 剥离）。全量 pytest **298 通过 / 45 失败**（基线 297 零回归，45 红全出 3 新文件）。**✅ D-03/D-07/D-10 后端施工完成（backend，2026-08-13）**：① D-03 sections.py DATA_PATTERNS（:148-185）补 F 家族前缀形态（`^[*+]?F\d+:`/`F\d+[XYZ]:?`/FIP/FIR/FIC/FM/FC）+ 辅助卡缺口词条（Cn/DE/DF/FS/SD/CF/SF/EM/TM/CM/TF/DD/DXT/SB/DS/SC/ELPT/NOTRN/TALNP/MPLOT/RAND/FILES/IDUM/RDUM/FMESH），节首直出未设计卡进 data_lines→other_cards 兜底；② D-07 parse_sdef_simple 裸分支白名单补 ERG/WGT/CEL/TME/EFF/RAD/EXT/DIR/X/Y/Z（:593-620）+ 裸 AXS/VEC 多值 + `POS=` 续值遇已知 key 停靠（:485-497，防吞裸 ERG/WGT）；③ D-10 parse_data_cards 6 处 other_cards append 改存 raw_line（:1044/:1085/:1090/:1154/:1286/:1317）保留行内 `$ 注释`。回归测试 tester-dfix 红基线 45 红→**全绿**（D-03 29 / D-07 12 / D-10 5），全量 pytest **343 绿 / 0 红**（298 预修通过 + 45 红转绿），R1/R2-R4/契约漂移闸门/样例冒烟全绿。改动清单 docs/backend-changes.md 附录 F。
>
> **D-03 范围已扩**（tester-fb-accept 验收新发现，同根因）：紧凑 INP（无空行分隔）下 `*F4`/`+F4`/`F5Z:P` 被 split_sections 误分曲面段丢弃 → DATA_PATTERNS 补 `^[*+]?F\d+`/`F\d+[XYZ]:?`。**✅ D-05 前端乘子 UI 已完成（frontend-fb1，2026-08-13）**：TallyTab.tsx 本地 Tally 接口加 multiplier + push/pull/addTally 全链路透传 + thead/tbody 加「乘子」列与单行输入框（placeholder "如 8.65E10 1 -5 -6"，空值不生成 FM 卡）；gui/src/utils/DeckContext.tsx TallyDef 加 `multiplier?: string`（与后端 models.py:200 字段对齐，api_server 358/480/665 同 key 读写，生成器 inp_generator.py:648/671 空值不发 FM）。**偏差说明**：指令写 contract.ts 但 tally 类型实际在 DeckContext.tsx（contract.ts 无 tally 类型），已落位正确文件。vitest 19/19 + tsc EXIT 0 + build EXIT 0。改动记 docs/frontend-changes.md。**D-05 闭环 = #1 FM 导入识别+可编辑 全链完成**。**✅ #1/#6 验收通过 + #2 闭环（tester-fb-accept，2026-08-13）**：6 回归用例全真守卫无删弱（含真实 parse_data_cards/generate_tallies 兜底）；全量 pytest **297 绿 / 0 红**（287+6 D-01+4 #2）；真实 HTTP 联调全过（text-to-section cells 字段全保留 / parse-inp FM5 multiplier 带出 / generate 回放 `FM5  8.65061E10 1 -5 -6` 再 parse 保留 / photon_skyshine.inp 端到端 f5z:p→F5+multiplier 保留）；#2 前缀已支持往返不丢（test_regress_fm_prefix.py 4 用例）。**数据卫生**：§8 变更日志第 252 行 D-01 与 #1/#6 行并发粘连已由 PM 修复。
>
> **✅ D-01 已修复（backend，2026-08-13）**：回归测试 `tests/parser/test_regress_independent_si_sp.py` 6 用例由 4 红 2 绿 → **6 全绿**。改动：core.py SI/SP 分支（原 :1101-1102）——①最小修复：SI/SP 行 append 进 `other_cards`（保底不丢，round-trip 保真）；②增强修复：`source_mode=="surface"`（SSW/SSR 面源）时并入结构化分布（新增 `_merge_sisp_entry` :158 按 id 合并，复用 `_parse_sisp_structured`，与 SDEF 分支一致；source_mode 保持 surface）。安全性：surface 模式生成器只走 SSW+SSR 忽略 sdef_distributions → 无双重发射；other_cards 末尾无条件回放 → round-trip 保真。全量 pytest **293 绿 / 0 红**（287 + 6 D-01）。改动清单 docs/backend-changes.md 附录 E。tester-fb-accept 待复核 #1/#6 与 D-01；arch-lexicon 重锚定 C810.pdf 进行中。
>
> **✅ 词条审计已完成并通过（arch-lexicon，2026-08-13）**：`docs/contracts/card-lexicon.md`（权威词条目录=单一事实来源）+ `docs/card-lexicon-diff.md`（差异表+施工契约）。**7 条差异**：D-01【P0】独立 SIn/SPn 静默丢弃（core.py:1101-1102 startswith→i+=1 不进 other_cards；触发 SSR 面源分布 / SDEF 与 SI 隔 C 注释）；D-02【P1 结构/P2 实务】计数辅助卡体系无结构化关联（Cn/DE/DF/EM/TM/CM/CF/SF/FS/SD/FU/TF/DD/DXT/SPDTL/FT/FQ/FC，均 other_cards 原样保留不丢，无 UI 可保原样仅登记）；D-03【P1 边界】sections.py DATA_PATTERNS 缺 Cn/DE/DF/FS/SD/CF/SF/EM/TM/CM/TF/DD/DXT/SBn/DSn/SCn/ELPT/NOTRN/TALNP/MPLOT/RAND/FILES/FMESHn → 节首辅助卡误分曲面/栅元段（常规布局无影响）；D-04【P2】_KNOWN_OTHER_CARDS 精确匹配失效（FMESHn≠FMESH，数据仍保留仅 known 语义落空）；D-05【P2】FMn 前端无乘子编辑 UI（后端已全链修，api 已带 multiplier，可派前端 TallyTab 加乘子框零后端改动）；D-06【P2】En(n≥1) 参数化不解析只存原文（round-trip 不丢）；D-07【P2】SDEF 裸参数白名单缺 ERG/WGT/CEL/TME/EFF/X/Y/Z/RAD/EXT/AXS/VEC/DIR。已知线索核查：FM 已修（工作树）、FS/FT/FQ/SD/CF/TF/DE/DF/Cn/E4/T4 均保留不丢（非漏识别）、E4/T4 已识别。**施工排期**：P0 D-01 最小修复（core.py:1101-1102 改 append other_cards + 增强 SSR 场景并入结构化分布）优先，backend-fb16 收尾后派后端；P1 D-02（登记型）/D-03（DATA_PATTERNS 补词条）；P2 D-04/D-05（前端）/D-06/D-07 可选。验收基线 pytest 287 不破。
>
> **⚠️ 权威源纠正（2026-08-13 上级）**：MCNP 卡类型**唯一权威 = 官方 `D:\MCNP\MCNP6\C810.pdf`**（已确认存在）；`app/docs/` 蒸馏 md 是**派生产物，可能有盲区**（FM 就是 md 漏掉的），不能作单一事实来源。已派 arch-lexicon **重锚定**：词条目录/差异表逐条对照 C810.pdf（Read 按页读）重新校验；权威源标注=C810.pdf、md 标"派生产物"；顺带补齐 md 与 PDF 差异（漏的卡补进 md）；系统性修复仍按差异表但以 PDF 为准；文档开头"单一事实来源"声明改为"C810.pdf 为唯一权威源，本文件与 md 均为派生需随 PDF 更新"。待重锚定汇报。
>
> **✅ C810.pdf 页级核验完成（tester-pdf，2026-08-13，零安装用本机 fitz 1.28.0）**：1001 页全文抽取成功。**关键纠正：C810.pdf 实为 MCNP5 卷 I+II 全文 + MCNP6.1/MCNP5 发布说明 + 附录 + 索引；卡格式权威章 = MCNP5 卷 II Ch.3（PDF 页 526-691，打印页 3-1~3-166），本 PDF 无 MCNP6.1 手册卷 II Ch.3**——MCNP6 专属卡在本 PDF 无格式定义。Table 3.11（3-161~3-164）= 完整卡汇总表。**D-09 ~35 族三分定案**：①确认真卡 27 族（带 C810 打印页号：AREA/ESPLT/TSPLT/PWT/EXT/VECT/FCL/WWE/WWN/WWP/WWG/WWGE/MESH/PDn/DXCn/BBREM/VAR/URAN/SPDTL/DRXS/TOTNU/NONU/AWTAB/XSn/PIKMT/MGOPT/THTME，另 ZA/ZB/ZC 在 Appendix A/F 承认为卡、ZD 无）；②MCNP6 专属 8 族（BFLD/COSY/DAWWG/EMBED 族 8 子卡/KPERT/KSEN/TROPT/UNC）；③无证据 16 族（BFLCL/COSYP/DMn/FMULT/HISTP/KOPTS/LCB/LCC/LEA/LEB/OTFDB/STOP/WWGT/WWT/ZD/MXn，PyMCNP 类多空 docstring=占位）。**diff 新增 D-11（结构鉴定+三分定案）/D-12（PDF-only 20 族 Ch.3 真卡待登记：AREA/EXT/VECT/PWT/VAR/TSPLT/BBREM/FCL/PDn/DXCn/DRXS/AWTAB/XSn/PIKMT/MGOPT/THTME/URAN/WWG/WWGE/MESH）/D-13（词典-only 复核：ACT/BURN/PHYS:H/HE=MCNP6 专属，MPHYS/LCA=C810 全文 0 出现疑似占位）**。结论：核验成功，全部 P2 文档层不阻塞。已派 arch-lexicon 补录 D-11/D-12/D-13 进 lexicon 正式词条（按 C810 页号）+ 权威源声明修正为"MCNP5 版卡格式，MCNP6 专属卡另以 MCNP6 手册卷 II Ch.3 核验（本机无该手册，标注待核验）"。
>
> **🚨 红线强化（2026-08-13 上级）**：**禁止安装任何东西**（poppler-utils、pip 包、npm 包一律不装）。tester-pdf 只许只读探测本机已装 PDF 库；无可用库立即停上报；**C810.pdf 核验放弃装 poppler 选项**，若无现成库可抽文本 → **核验改为用户手动抽查**（用户本机有 PDF 查看器，PM 列关键条目清单让用户翻 PDF 确认）。所有涉及环境依赖的操作先问用户，默认禁止安装。
>
> **⚠️ 设计意图校准（2026-08-13 上级，词条专项目标重定向）**：程序**有意**不覆盖所有 MCNP 关键词——未设计"数据卡"关键词 → **other_cards 兜底**（设计好的安全网，非遗漏非 bug）；未设计栅元卡关键词 → 栅元 **other_params 框**接住。**词条专项真正价值重定为三件事**：①**兜底网可靠性**（任何未设计卡完整落其他框、round-trip 不丢不坏，重点测几条）；②**告警机制**（known/unknown 提示合理，用户知道"未结构化支持、原样保留在高级-其他"）；③**高价值卡结构化**（只对用户高频依赖的卡加结构化，如 FM 已修——导入识别+可编辑；**不是所有卡都结构化**）。D-09（~35 PyMCNP 家族落 other_cards）**不是缺陷**是设计行为→降 P2/P3 兜底可靠性验证。D-02~D-07 逐条重评为三类：兜底可靠可保持（仅登记）/ 兜底会丢会误吸收=真需修（P0/P1）/ 高价值卡结构化（仅高频，参照 FM）。词条目录价值保留（"程序认识哪些卡 + 哪些走兜底"文档，供 UI 提示/告警/文档），**不再以全结构化为目标**。已转达 arch-lexicon 重写 card-lexicon.md 修复目标 + 重评 D-02~D-09 + 更新 diff 优先级；tester-pdf 继续核验 PDF（供告警/文档）。汇报须明确"确认兜底即可" vs "真需要修"。
>
> **✅ 重锚定完成（arch-lexicon，2026-08-13，含诚实限制声明）**：**C810.pdf 页级核验未能执行**——本机缺 poppler（pdftoppm 未装）无法逐页读卡，不虚构核验数字。已完成三源交叉：蒸馏 md（各带 C810 页号引用）+ 解析器源码实测 + PyMCNP 交叉核对。card-lexicon.md 权威源标注已改（唯一权威=C810.pdf，本文件+md=派生），加"核验状态"诚实声明；md 补录计数辅助卡 14 个（FCn/FQn/EMn/TMn/CMn/CFn/SFn/FSn/SDn/FUn/TFn/DDn/DXT/SPDTL）；**澄清：FM 卡在 md 中已有（C810 md §4b:305-312），原失识别在解析器不在 md（纠正 PM 之前说法）**。差异 1-7 保持（解析器行为事实），**新增 2 条**：D-08（P2，已修：md 主清单缺计数辅助卡已补录）、D-09（P2，PyMCNP 交叉核对新增 ~35 族未收录卡 AWTAB/BBREM/BFLD/BFLCL/COSY/COSYP/DAWWG/DMn/DRXS/DXCn/EMBED 族/FMULT/HISTP/KOPTS/KPERT/KSEN/LCB/LCC/LEA/LEB/MESH/MGOPT/OTFDB/PDn/PIKMT/STOP/THTME/TROPT/TSPLT/URAN/UNC/VAR/WWG/WWGE/WWGT/WWP/WWT/XS/ZA/ZB/ZC/ZD/MXn/AREA——全部落 else→other_cards 原样保留无数据丢失，非 P0/P1，仅文档盲区+known 告警缺口）。**差异表现 9 条 = P0×1 + P1×3 + P2×5**。D-01 已由 backend-fb16 修复（293 绿）。**PM 决策（2026-08-13）**：①审计通过，D-02/D-03（P1）待 PDF 核验结论后统一排后端；②已派 tester-pdf 尝试用本机已装 Python PDF 库（pymupdf/PyPDF2/pdfplumber）抽取 C810.pdf 文本做卡类型索引核验（**红线：不自动安装**），若本机无 PDF 库则上报上级决定是否装 poppler。
>
> **✅ PyMCNP 交叉核对维度（2026-08-13 上级补充）**：`D:\MCNP\PyMCNP`（本项目 pymcnp 依赖源码）`src\pymcnp\inp\` 每张卡一个类（774 个文件，Act/Arb/Awtab/Bbrem/Bflcl/Bfld/Cf/Cm/Cosy/Cut/De/Df_0/Df_1/Dm/Drxs/Ds_0/Ds_1/Dawwg/Dbcn/Dd 等），是**系统性组织的卡类型清单**，作为词条目录**交叉核对清单**。**权威源仍=C810.pdf，pymcnp 只作交叉参考**（本身也可能有缺口，不替代 PDF）。**比对三维度**：C810.pdf（权威）↔ PyMCNP inp/ 卡类清单（交叉参考）↔ 解析器实际识别清单 → 完整差异表。已转达 arch-lexicon（重锚定 C810.pdf 过程中同步交叉核对，新增/修订差异补进 docs/card-lexicon-diff.md，汇报单列"PyMCNP 交叉核对新增条目"）。注意别把结构性类（Cell/Area/Comment 等）误收进词条。**D-01 回归测试已建（tester，2026-08-13）**：`tests/parser/test_regress_independent_si_sp.py`（6 用例：SSR 场景 2 + SDEF 隔 C 注释场景 2 + 对照 2）。当前 4 红 / 2 绿（对照绿）——4 红即缺陷证据，后端修复后须全转绿作 D-01 验收基准。全量 pytest 289 通过 / 4 红（预存在 287 基线零回归，4 红全出此新文件）。

> **P0 根因（tester-fb1 2026-08-13 复现确认，断点均在后端）**：
> - **#6 cell 文本→表单丢数据（最严重）**：`api_server.py:714` `_handle_text_to_section` shell 包裹把用户 cell 文本追加在曲面段后 → 落数据段 → `split_sections` 归 data_lines → `parse_data_cards` 兜底 other_cards，用户全部栅元字段丢失（只剩假栅元 "1 0 -1"）。materials/tally 分支正常（本属数据段）。对照组证明 parse_cells 字段解析完整，勿改。修法：cells 分支 shell 把文本放栅元段（如 `C  shell\n{text}\n\nC  surf\n1 pz -1e9\n\n`）或独立解析路径。
> - **#1 FM 卡不识别**：全链无 multiplier 概念。入口门（core.py:1001-1003）正则要求 `F\d+`，"FM5" 不匹配→other_cards；parse_f_tally（core.py:704-786）无 FM 分支；TallyDefinition（models.py:174-199）无 multiplier 字段；生成器（inp_generator.py:632-668）只发 F 卡。修法：入口门扩 `^[*+]?FM\d+` + FMn 解析乘子附加到同 number 的 TallyDefinition + models 增 `multiplier: str=""` + 生成器 FMn 卡输出（参考 app/docs/MCNP6_FN卡结构参考.md + C810_卡片格式详细.md）+ `_deck_to_frontend_dict`（api_server.py:444）携带 multiplier。前端乘子 UI 由 PM 另行派前端（等后端 API 结构定）。
> - **验收基准**（tester 已建，修完须转绿）：`tests/parser/test_regress_fm5_import.py`（3 用例）+ `tests/parser/test_regress_cell_text_to_form.py`（3 用例）。基线 pytest 271→**277** +0 失败。

> **🚨 硬性红线（2026-08-13 用户明确）**：**任何脚本/验证/施工不得自动运行 npm install / npm ci / pip install**。本机依赖已就绪，用户对"自动安装依赖"高度敏感。违反即打回。release.bat 门禁中的 `npx vitest run` / `npm run build` 不得触发依赖获取——若 node_modules 缺 vitest，须先确认/手动补齐，不得靠 npx 自动下载。
>
> **✅ 当前事件：用户暂停——彻底澄清闭环（2026-08-13）**：那批进程全部归因到用户环境，与 release.bat 构建**完全无关**：
> - **19 个 node.exe = MCP 服务器**（context7-mcp/memory/sequential-thinking/github/browsermcp，父进程均为 Claude Code 宿主机 cmd 链，多实例 = 多套会话）——用户环境基础设施。
> - **1 个 python.exe（PID 15548）= 已部署 MCNP 应用 sidecar**（`D:\MCNP\MCNP输入卡生成器\python.exe -u backend/mcnp_bridge.py`，父进程主 exe）——tester 复验启动的应用残留。
> - **无任何 npm install/pip install/PyInstaller/cargo 进程**——"自动安装依赖"彻底排除。
> - releaser 实跑从未执行（权限拦截）、无任何 install、部署目录未覆盖（仍 08-12 23:50 packager 产物）。建议不 kill（均非异常）；已部署 app sidecar 由用户决定是否关闭。
> - **release.bat 脚本本身不含 install 步骤**。**待用户确认后续**：是否继续 release.bat 实跑验证、脚本 commit 时机、build.bat 是否废弃。

> **当前执行计划：本地一键发版脚本（新需求，2026-08-12 上级转来）**：用户明确不搞 GitHub/CI，把手工 5 步打包流程固化为仓库根 `release.bat`。步骤：① 门禁 pytest 271 + vitest 19（失败中止）；② 版本号读 tauri.conf.json/package.json（当前 1.6.3，可选更新）；③ 构建 vite → pyinstaller sidecar → sidecar 替换 binaries → tauri build；④ 部署清空复制到 D:\MCNP\MCNP输入卡生成器；⑤ 自检 exe → 5001 探活 → 冒烟 → 清理 → 报告。**关键点**：spec 模块清单自检（preview_cache.py 曾漏——脚本核对 _internal/app 含它）；onedir 结构完整复制；每失败步明确错误 + 非零退出码。产出：release.bat + README-release.md + 实跑验证。**releaser（a6d5c31f2fddd97da）已派发：先写脚本 + README，实跑待 tester 复验完成后错峰执行（避免踩部署目录产物）。**
> - **✅ 重新打包完成并验收通过（2026-08-12，packager2 + tester）**：v1.6.3 带 GeometryTab 材料下拉修复重新部署 D:\MCNP\MCNP输入卡生成器。commit 链 773742a（GeometryTab.tsx）→ c6705cf（portalPosition.test.ts 6 用例）。5 步打包全成功（vite 2.79s → PyInstaller 1m24s → sidecar 0.7s → tauri build Rust 增量 25.77s → 部署 0.6s）。sidecar 自检全过（preview_cache.py 在位 / PyQt5 剔除 / vtk 剔除 / vendor/geouned 在）。新 exe 6645760B。**tester 复验通过**：exe 启动 + 5001 探活 + 3D 出图（缓存命中正常）+ **材料下拉修复已打包生效**（bundle grep 证据链：createPortal×2/document.body/zIndex 1100/1200/position:fixed/preview-overlay/mat-cell-btn/rgba(15,15,40,0.97) 全命中；clampDropdownLeft 压缩重命名消失属预期，同源 6 用例独立验证）。

> **当前执行计划：打包 v1.6.3 到 D:\MCNP\MCNP输入卡生成器（新任务，2026-08-12 上级转来）**：打包 = Tauri 流程（root build.bat 过时勿用）：gui npm run build → gui pyinstaller mcnp_sidecar.spec（必须 gui/ 下跑，onedir）→ sidecar 替换进 src-tauri/binaries/（保留 onedir）→ RUSTUP_HOME/CARGO_HOME=D:\rust npm run tauri build → 产物复制到 D:\MCNP\MCNP输入卡生成器。**⚠️ 关键缺陷：spec `_keep_py` 缺 `preview_cache.py`**（3D 预览修复新增，api_server.py:38 顶层 import），不打进 sidecar 则 exe 启动即崩——**必须先修 spec 再加 `"preview_cache.py"`**。另注意：spec 引用 D:\MCNP\GEOUNED（vendor，确认存在）；PyInstaller 排除 pyvista/vtk 勿改；版本 1.6.3。**打包 agent（packager ad073627eee69f7c2）已派发后台运行**，测试验证产物待打包完成后派发。验收：产物在 D:\MCNP\MCNP输入卡生成器、exe 启动后 5001 探活 + 3D 预览出图、_internal/app/preview_cache.py 存在。
> - **⚠️ Qt 残留清理（2026-08-12 上级核实，与打包同批）**：源码干净（commit 9bcbcaf 已移除 PyQt5 GUI 层，无 .py import Qt），但三处残留：① `gui/build_onedir/` 被 git 跟踪（旧 PyInstaller 产物含 PyQt5/Qt5 DLL，v1.6 过渡期误提交）；② `requirements.txt` 列着 `PyQt5>=5.15` 和 `pyvistaqt`（死依赖）；③ 现役 sidecar 打包进 PyQt5（spec excludes 漏 PyQt5）。**本次打包已把 PyQt5 加入 spec excludes 并一次构建生效**（_internal/PyQt5 已剔除，实测为 matplotlib 经 pymcnp qt_compat hook 传递带入，非 matplotlib backend 误伤）。**待办清理（打包验收通过后安排）**：git rm -r gui/build_onedir/ + requirements.txt 移除 PyQt5/pyvistaqt（确认无代码依赖后）+ 重跑 pytest 271 零回归。
> - **✅ 打包 v1.6.3 完成并验收通过（2026-08-12）**：产物已部署 D:\MCNP\MCNP输入卡生成器（MCNP 输入卡生成器.exe 6645248B + python.exe sidecar 25035105B + _internal/ 含 app/preview_cache.py、vendor/geouned/、无 PyQt5）。spec 两处修复（preview_cache.py 加 _keep_py + PyQt5 加 excludes）一次构建生效。5 步打包全成功（vite 2.74s → PyInstaller 2m12s → sidecar 替换 25s → tauri build 2m24s → 部署）。**tester 产物验证通过**（exe 启动 + 5001 探活 + 3D 预览出图 + spec 修复确认）。**spec 修复已 commit（d3d9db6）**。Qt 清理（cleanup）进行中。
> - **🖥️ 主侧直接完成前端改动 + 补测闭环（2026-08-12）**：`gui/src/components/GeometryTab.tsx`（唯一文件，未 commit）修复 cell 卡材料下拉被玻璃卡容器裁切（根因 td 内 absolute 被 .table-wrap overflow-x:auto 裁剪 + .glass-card backdrop-filter 层叠上下文）。修法：React portal → document.body + position:fixed + 点击外部关闭 + 右缘防溢出。**补测已闭环**：抽 `clampDropdownLeft(x, viewportWidth, dropdownWidth=200)` 纯函数（GeometryTab.tsx 模块级导出，等价原 `Math.min(x, (innerWidth||0)-220)`，tsc 过）→ `gui/test/portalPosition.test.ts` 6 用例全绿，**前端 vitest 基线 19/19**（原 13 + portalPosition 6）。
> - **✅ 重新打包 v1.6.3 完成并部署（2026-08-12）**：GeometryTab.tsx 材料下拉修复已带进 D:\MCNP\MCNP输入卡生成器。commit 链 **773742a**（fix(ui): material dropdown portal to escape glass-card clipping + clampDropdownLeft，只含 GeometryTab.tsx）→ **c6705cf**（test(ui): clampDropdownLeft portal position cases，含 gui/test/portalPosition.test.ts 6 用例，PM 授权提交）。5 步全成功（vite 2.79s → PyInstaller 1m24s → sidecar 替换 0.7s → tauri build 增量 Rust 25.77s+vite 3.13s → 部署 0.6s）。自检过：新 exe 6645760B（旧 6645248B，前端 bundle 已变）、`_internal/app/preview_cache.py` 在位、PyQt5/vtk 剔除、vendor/geouned 在。版本 1.6.3 未改、spec 两处修复未动。**待 tester 产物复验**（exe 启动 + 5001 探活 + 3D 出图 + 材料下拉修复在打包版生效）。上游已转新需求（本地一键发版脚本 release.bat，与产物无关）。
>
> 上一任务（3D 预览性能修复）**已完成并验收**：后端 271 绿/0 红 + 前端 vitest 13 全绿，契约验收目标全部达成（缓存命中 0.02s、bound 全中、farNear≤1e4），commit 链 97de569→e65d423→dbab84e→6e65c9e→d8d4013（perf/preview3d）。
> - **管线**：Preview3D.tsx → POST /api/preview-3d → api_server(_handle_preview_3d, :1001) → FreeCADEngine.build_geometry(freecad_preview.py:243) → 子进程 _freecad_csg_worker.py（逐曲面布尔裁剪 + 逐栅元 AST 布尔级联）→ tessellate(1.0) → 每栅元 STL → base64 进单 JSON → 前端 atob+STLLoader.parse 全量加载
> - **三因**：① 打开卡/黑屏（主因）：无缓存每次全量重建；base64 单 JSON 可达几十 MB（api_server.py:1058）；前端 JSON.parse+atob+STLLoader.parse 主线程同步无 Web Worker（Preview3D.tsx:305-308）；后端 120s 硬超时（freecad_preview.py:339）。② 交互低帧率：相机 change 每帧 rebuildTicks 新建数百 Line/Sprite/CanvasTexture 不 dispose（Preview3D.tsx:196-252）；渲染循环无条件每帧跑（354-383）；全栅元 transparent+depthWrite:false overdraw（312）。③ 闪烁/错乱：near=0.1/far≈35万（99,344）；无几何居中/归一化 target 恒原点（341）；大坐标 OCC 浮点容差。
> - **20m 放大器**：bound 自适应 `_compute_bound_from_surfaces`（freecad_preview.py:206-225）：2000 单位→B≈2700 布尔裁剪更贵；预览无单位缩放/无归一化（仅 STEP 导出有 SCALE=10）；前端深度精度失衡。
> - **执行流程**：复现验证已由 `repro` 完成 → **架构师契约已锁定并经 PM 裁决生效（docs/contracts/preview3d-performance.md）** → **后端/前端施工并行派发中** → 测试复核（补大尺寸预览测试 + 全量 251 绿不破）。**性能任务与 P0/P1/P2 正交，施工不得破坏现有测试（每步全量 pytest）。** 契约要点：P0a 打开卡（后端 preview_cache 深模块 + 前端加载态，缓存命中打开 ≤1s）、P0b 交互卡（TickGrid/renderGate/cellMaterial 拆深模块 + vitest）、P0c 深度（computeCameraParams 几何归一化 + far/near ≤1e4）、P0d 新发现三项（IPv6 前端改 127.0.0.1 / vtk 惰性 / bound 位移参数修正）。
> - **PM 三项裁决（2026-08-12，已闭合生效）**：① 半透明默认 opaque + "半透明查看"开关默认关（附非阻断引导提示可选）；② vitest 批准为 gui/ devDependency（测试目录 gui/test/ 独立，不并入 pytest 门禁）；③ 冷启动 ≤3.0s 批准（核心指标 = 缓存命中 ≤1.0s）。
> - **施工完成（2026-08-12，双线交付）**：分支 perf/preview3d（基于 refactor/generator-tech-debt，251/0 基线）。
>   - **后端（commit 97de569 → e65d423 → dbab84e → d8d4013）**：bound 修正（实测 shield_20m 5300→2700 / stress_bunker 2050 / inp01 13100 / inp09 3665.6 全中契约区间）+ app/preview_cache.py 深模块（LRU 3，自有拷贝）+ vtk 惰性化 + handler 缓存接线 + clear-stl evict 联动。全量 pytest **271 绿 / 0 红**（251 零回归 + 新增 20：bound 8 + cache 8 + worker 4）。真实 FreeCAD e2e 17/17：响应四字段不变、截面复用正常、KPI 未命中 0.68s→命中 0.02s（≤1.0s 达成）、clear-stl 无悬挂。
>   - **前端（commit 6e65c9e）**：api.ts 127.0.0.1（14 文件 ~25 处 localhost 归零）+ 4 深模块（TickGrid/renderGate/cellMaterial/cameraParams）+ 加载态 + 半透明开关 + vitest 4 文件 13 用例全绿。tsc EXIT 0、build 通过。onKeyDown 语义回归已自查恢复。
>   - **✅ 3D 预览性能修复完成并通过复核（2026-08-12）**：tester 步 6 复核通过。后端全量 pytest **271 绿 / 0 红**（251 零回归 + 新增 20）；前端 vitest 4 文件 13 用例全绿；纪律核对全过（251 零改动、api_server 路由 0 增删、漂移闸门 7/7、无新运行时依赖）；联调点实测全过（miss/hit 四字段一致、real fixture cross-section 复用 slices=1/polygons=2）。契约验收目标全部达成：打开缓存命中 ≤1.0s（KPI 0.02s）、拖动对象 ≤60/纹理 ≤30、farNear≤1e4、bound 全中（2700/2050/13100/3665）、vtk 惰性、IPv6 归零。非阻塞观察：worker 对退化输入产空 STL 属既有特性，与本次改动无关，已记录待将来排查。commit 链：97de569→e65d423→dbab84e→6e65c9e→d8d4013（perf/preview3d）。
> - **前端施工完成（2026-08-12，frontend-perf a2b6642aaf0b9b4c2）**：步 3 `gui/src/utils/api.ts`（127.0.0.1 单一常量）14 文件 ~25 处 localhost:5001 归零；步 4 `gui/src/three/` 4 深模块（TickGrid 台账+完整 dispose+步长表 1e6 / renderGate dirty 按需渲染 / cellMaterial 默认 opaque / cameraParams farNear≤1e4）+ Preview3D.tsx 接线 + 几何归一化（translate 先于 computeBoundingBox）；步 5 加载态遮罩 + 半透明查看开关（默认关，M0 真空 opacity 0 不变）；vitest 4 文件 13 用例全绿（`cd gui && npx vitest run`），tsc/build 通过。改动清单 docs/frontend-changes.md。
> - **复现实测结论（repro，2026-08-12，部分反驳原诊断）**：
>   - **被反驳**：前端同步解析 16MB=54ms（非主因）；子进程最大 2.38s（无 120s 超时风险）；±2742/±10000 布尔 <0.6s（无 OCC 崩溃）；bound 放大布尔不慢。
>   - **真凶**：① 打开卡（每次无缓存全量重建 3.30s + 无加载态 + handler 1.3~2.7s → 1.5~3s 白屏）；② 交互卡（rebuildTicks 每帧 ~160 对象/81 GPU 纹理 + 全栅元透明 overdraw + 无条件渲染循环，与几何大小无关）；③ 大坐标闪烁（far/near 失衡，±10000 时 17.5M 超 2^24 深度极限，target 恒原点）。
  >   - **新发现 3 项**：IPv6 HTTP 惩罚（server 绑 0.0.0.0:5001 + 前端 localhost，Chrome Happy Eyeballs 每请求 300-500ms，影响全部 25 端点，修法绑 [::]:5001 或改 127.0.0.1）；vtk 惰性化（worker 每次 import vtk 白付 0.46s 占子进程 ~35%）；bound 过撑 bug（把宏体方向向量/轴长当坐标，shield_20m 轴长 4000→B=5300 vs 真实 2000，只对位移参数取 max）。
> - **大尺寸预览 fixture 已 vendor 7 个**（tests/fixtures/）：preview_inp09_m27.inp / preview_inp01_m100.inp / preview_duct_conc.inp / preview_inp96.inp / preview_inp08_m27.inp / preview_shield_20m.inp / preview_stress_bunker.inp。复现脚本在仓库外 D:\code\preview_measure\（不污染生产）。
> - **命名冲突教训（2026-08-12）**：曾以 `backend` 命名派发复现 agent 导致名称解析冲突，`repro` 正确执行后也被停（用户主动）。**后续新增 agent 用唯一名（如 arch-perf/repro）**，避免与在途 agent 重名。
>
> 上一计划（P0 测试防线 + P1 技术债清偿 + P2 文档补齐）**已完成并验收**：全量 251 绿/0 红。

> **当前执行计划**：测试补防 + 技术债清偿 + 文档补齐（P0 测试 → P1 重构依赖 P0 → P2 文档并行）。
> 定稿计划文件：`C:\Users\13789\.claude\plans\tranquil-greeting-biscuit.md`（优先级由上级拍板并授权自动执行）。
> - **P0 测试防线**：派「测试」（**第一轮完成，2026-08-12**）：234 通过 / 17 失败。M1.1/M1.2/M1.3/M1.5 通过，**M1.4 未全绿——门禁拦截**。17 失败全部为"按设计先红"的缺陷/技术债 pin：F-A~F-E 是引擎真实缺陷（测试网捕获），F#3/F#7 是技术债（P1 处理）。
> - **P0 修复轮（当前：后端已完成 F-A~F-E 重新施工，2026-08-12）**：
>   - 契约 **docs/contracts/bugfix-f1-f5.md（已确认有效，2026-08-12 上级裁决微调完成）**：F-A~F-H 均为基线真实缺陷，放行重新施工。F-A 裁决方案 C（解析器拦截节头为主 + banners.py 单点辅助）、F-B 解包 CellRow、F-C 正则扩逗号、F-D options 上移 M 头行、F-E 剥 &、F-F/F-G/F-H 翻新 pin（本轮可选不阻塞）。**微调：多源 SDEF 漂移（F#5/F#6）归 P1 不并入本轮；范围=F-A~F-E；终态 245 绿/6 红；kitchen-sink R1/R4 移入 P1 验收，复合根因清单写入 §0.5.5**。
>   - **施工结果（2026-08-12）**：全量 `python -m pytest tests/ -v` = **245 绿 / 6 红**（与 §0.5.3 预期一致）。F-A~F-E 全部红转绿；F-F/F-G/F-H 已翻新 pin 为正确行为断言（仍绿）；6 红 = 4 技术债（F#3/F#7）+ 2 kitchen-sink（R1/R4）。**banners.py 词汇按"生成器全部节头"扩展**（契约 11 词汇外实测发现 Fission/KCODE/KSRC/Additional 等内联 C 头也泄漏）；**prob41c R1 伴生修复**：normalize_lines 缩进合并保留续行 `$` 注释。**app/UI_ARCHITECTURE.md 基线为空文件（0 字节）**，§8.1 无法重锚定；**已由架构师补建完成（2026-08-12，见 §8 变更日志）**。改动清单见 docs/backend-changes.md。
>   - **基线真相（上级已承认方法论失误）**：基线全量 **234 通过 / 17 失败**，与 QA 报告一致，契约依据成立。上级上一轮"245/6"复核错误源于读了后端施工中途未 commit 的工作树。
>   - **上级最终裁决（2026-08-12）**：
>     1. 契约有效，放行后端按 bugfix-f1-f5.md 重新施工
>     2. **M1.4 门禁（本轮）**：minimal + 3 样例（prob41c/avr13/inp24）R1 全绿 + F-B/F-C/F-D/F-E 红转绿；**kitchen-sink R1/R4 本轮允许保持红，归入 P1 F#5/F#6**（已实测：修完 F-A~F-H 后 kitchen-sink 仍不会绿，SDEF 漂移需 P1 重构）
>     3. 技术债 F#3/F#7 的 4 红保持红（设计内 pin，P1 范围）
>     4. **本轮终态预期 245 绿 / 6 红**（4 技术债 + 2 kitchen-sink）；P1 放行条件 = 本轮达成 245/6；P1 目标 = 6 红全部转绿
>     5. 施工纪律不变：每步全量、禁改断言骗绿、按 §7 不越 P1 边界、完成按 §8 重锚定
>   - **执行链（M1.4 已通过，P1 放行条件达成）**：
>     - 架构师微调契约完成 → **后端重新施工完成**（全量 **245 绿 / 6 红**，与契约 §0.5.3 一致）→ **tester 复核通过（2026-08-12）**：245/6 复跑两次稳定；6 红 = 4 技术债（F#3 x2、F#7 x2）+ 2 kitchen-sink（归 P1）；门禁 §0.5.2 全过（minimal+3 样例 R1 全绿、F-B/C/D 转绿、129+73+7 零回归）；无断言降级（用例总数 251 不变）；P1 范围未触碰（_wrap_long_lines/_generate_multi_source/_generate_distribution_sdef 均未改）；F-F/F-G/F-H pin 翻新写死正确行为断言全绿。
>     - **kitchen-sink 归 P1 实证**：头泄漏已归零（2102→2266 缩至 2102→2110，无 cell 注释污染/曲面膨胀），残留差异纯化为多源 SDEF 字段重组漂移（POS=F D1→X=F Y=D1、TME=D6→TME=0.0、SI1 V→L、分布注释丢失、SI 值空格归一化）= P1 F#5/F#6 验收基准。
>     - **P1 放行条件已达成**（上级裁决：本轮 245/6 即可放行 P1，P1 目标 = 6 红全部转绿）。**待上级确认后启动 P1**：架构师出重构契约 → 后端在 refactor/generator-tech-debt 分支按 F#7→F#3→F#4→F#5+F#6→F#1 顺序重构。
>   - **⚠️ PM 失误记录（2026-08-12，已修复）**：此前为回滚引擎代码执行 `git checkout -- app/` 时，误将架构师 P2 补全但**未 commit** 的 `app/UI_ARCHITECTURE.md` 还原成 git 空版本。docs/ 契约文件（api.yaml、bugfix-f1-f5.md）未受影响。**已由架构师（ae059169a61adc2ad）重建完成（2026-08-12，7 节非空，行号全 Grep 实测）**。关键重锚定行号：raw_overrides 守卫 1069(cells)/1081(surfaces)/1107(materials)/1115(sdef)/1131(phys)/1139(tally)/1147(e0)/1168(cut)、raw_tally 变体 1156、门控 1157/1162、_wrap_long_lines 1010、**F#3 import json 631/671（注意 673 是 `_json.loads` 调用行，非 import；PM 原记录 631/673 有误，已修正）**、F#4 247（Dn 254-290/普通 292-310）、F#5 375、F#6 384-403/434-449/486-489、F#7 110、E0DBG（core.py 826-827/1018-1021、__init__.py 138-140、api_server.py 608-610）。文档已含缺陷修复行为变化（banners.py/normalize_lines 剥 &/多粒子正则/小写 m/解包 CellRow/options 上移）。
> - **P0 复核口径（2026-08-12 上级裁决后，后端施工时须满足）**：终态预期 **245 绿 / 6 红**。本轮 11 例红转绿 = F-A 样例 R1 6（minimal/prob41c/avr13/inp24 R1 归零：prob41c ~1097、avr13 ~644 稳定）+ F-B 3 + F-C 1 + F-D 1；6 红 = 4 技术债（F#3/F#7）+ 2 kitchen-sink（R1/R4，复合根因移入 P1 F#5/F#6 验收，清单见契约 §0.5.5）。误判断言走 tester→PM 仲裁通道，绝不自行改断言。
> - **P1 技术债清偿（已完成并通过复核，2026-08-12）**：上级确认 P1 启动（放行条件 245/6 已达成）。**架构师 P1 重构契约已锁定（docs/contracts/p1-refactor.md）**。**后端施工完成**：全量 pytest = **251 绿 / 0 红**（复跑 ×2 稳定），6 红全部转绿。commit 索引：F#7 bf0a2c7 → F#3 c774e56 → F#4 52ca251 → F#5/F#6 4a e404172 → 4b/4c 018ced5 → F#1 1488aae（详见上文）。重锚定完成（review_findings 7 项 resolved/UI_ARCHITECTURE/test_tech_debt/PROJECT_MEMORY）。**tester 复核通过（2026-08-12）**：251 passed/0 failed 复跑稳定；逐 F# 核对全绿；纪律核对（用例总数 251 不变、无 skip/xfail 骗绿、api_server 路由/api.yaml 漂移闸门 7/7 绿含真实 HTTP、_wrap_long_lines/structured_distributions join 未进 P1 diff、无新依赖）；kitchen-sink 实证 5 项残留差异全消除（POS=F D1 保持/TME=D6 保持/SI1 V 型保持/概率键控注释保留/SI 值字节恒定，g1==g2 精确相等 len 2099==2099）。
> - **✅ 全量计划完成（P0 测试防线 + P1 技术债清偿 + P2 文档补齐，2026-08-12）**：全量 pytest = **251 绿 / 0 红**。最终交付：tests/ 测试网（unit 129 + parser 73 + integration 含契约闸门 7 真实 HTTP）、dev-requirements.txt、3 样例 fixtures、docs/contracts/（api.yaml 25 端点 + bugfix-f1-f5.md + p1-refactor.md）、app/UI_ARCHITECTURE.md（7 节）、review_findings.json 7 项全 resolved、引擎缺陷 F-A~F-H 全修、技术债 F#1~F#7 全清偿。分支 refactor/generator-tech-debt（P0 基线 996d11f 已并入）。**待上级确认可交付状态。**
> - **P2 补文档**：派「架构师」（**已完成，2026-08-12**）：app/UI_ARCHITECTURE.md 7 节齐全 + docs/contracts/api.yaml（OpenAPI 3.0，25/25 端点，每 path 带 operationId，已验收）。
> - **关键红线**：测试不得 import gui.backend.api_server（模块级 pyvista/FreeCAD 探测污染）；raw_overrides 行为兼容前端只写 4 key + 空串=无覆盖 + 1145 门控，禁止凭直觉改；往返断言用 R1 不动点而非文本相等；样例 inp24 已 vendor 进仓库副本（上级提供的官方测试库 D:\MCNP\MCNP6\MCNP_CODE\MCNP6\Testing）。
> - **契约要点（架构师已核）**：run-mcnp 响应 status 字面量是 "started" 非 "ok"；import-step/export-step/set-freecad-path 守卫分支 HTTP 200 下返回 status:"error"；validate-zaid/xsdir-search/serve-file 从 query 读参（GET），其余 22 端点为 POST body；do_GET/do_POST 同路由。闸门 HTTP 往返断言勿按统一信封假设。


## 1. 项目身份
- **名称**：MCNP 输入卡生成器（MCNP Input Card Generator）
- **版本**：1.7.0（已打包部署交付，commit fcc935b，2026-08-14；网格计数可视化功能随此版发布）
- **技术栈**：
  - 前端 UI：React 18 + TypeScript + Vite（端口 1420，表单化标签页界面）
  - 3D 渲染：Three.js（3D 预览）/ SVG（平面截面）
  - 窗口外壳：Tauri 1.x（Rust，无边框自定义窗口），另有 Electron 备用壳
  - 后端：Python 标准库 `http.server`（端口 5001，**不用 Flask**），PyInstaller 打包 sidecar
  - 核心引擎：pymcnp（BSD-3-Clause）+ 自研 generator/parsers
  - CSG 几何：FreeCAD（LGPL）；STEP→MCNP：GEOUNED（EUPL-1.2，随程序 vendor 打包）
- **核心业务**：用可视化表单 GUI 替代手工编辑 MCNP `.INP` 输入文件；覆盖生成/导入/校验/3D 预览/截面/材料库/源/计数/输出分析全流程
- **许可**：All Rights Reserved（自有代码禁止盈利/未经许可发布）；开源组件各按自身许可


## 2. 当前状态快照
- **开发阶段**：开发中（实验分支 experiment/geouned，v1.6.4 已具备完整可交付功能；网格计数可视化契约已锁定待排期施工）
- **当前分支**：`experiment/geouned`（主分支 `main`）
- **工作区**：有未提交改动（⑥ **MESHTAL 解析 Bug 2 修复（backend，2026-08-15）**：meshtal_parser.py 加 out=ij/ik/jk 二维矩阵解析 + col 改列头识别 + 文件头横幅感知 + 成功解析 warnings 恒空（删误导性 pymcnp 警告）+ meshtal_cache.py manifest 版本号 `_MANIFEST_VERSION=2`、tests/unit/test_meshtal_matrix.py +5 + test_meshtal_parser.py +1 + test_meshtal_cache.py +1、vendor real_meshtal_{jk,ij,ik,col,jk_multi}.meshtal 5 个真实 MCNP6 输出 fixtures、docs/backend-changes.md §N/N.7、pytest 461/0——见 §8 变更日志；⑤ **FMESH 关键字等号可选解析修复（backend，2026-08-15）**：fmesh_parser.py `_KEY_RE` 等号可选 + `_is_boundary_token` 边界判定（已知关键字/卡族头/`key=` 形未知关键字）+ `inc=` 未知关键字容错记 raw 兜底、tests/parser/test_fmesh_parser.py +5、vendor 官方 case1~5.i 进 tests/fixtures/、docs/backend-changes.md §M、pytest 454/0——见 §8 变更日志；① **FMESH 卡 FACTOR 字段后端支持**：models.py `FmeshDefinition.factor`、fmesh_parser.py `_KEYS`/`_card_lines`/`_has_structured`/CMESH 清空、api_server `_fmesh_from_list`，tests/parser/test_fmesh_parser.py +4、docs/backend-changes.md §L、pytest 449/0——见 §8 变更日志；② FMESH 表单改进字段对齐 MCNP6：models.py `eints→emints`/`t_ints→tmints` + 新增 axs/vec/tr、fmesh_parser.py `_KEYS`/`_card_lines`/GEOM 连写、core.py `_is_fmesh_body_line` TMESH= 误判修复、api_server `_fmesh_from_list` 新字段 + 向后兼容、tests +5、docs/backend-changes.md §K；③ expand-formula is_weight 参数扩展：api_server.py + api.yaml + tests/integration/test_api_expand_formula.py + docs/backend-changes.md §J；④ **FMESH 表单改进前端落地**：fmeshState.ts 字段改名 emints/tmints + axs/vec/tr + GEOM/OUT 选项常量 + 校验纯函数 validateFmeshRow/vectorsParallel、FMeshForm.tsx GEOM/OUT 下拉 + AXS/VEC 条件显示 + TR + MAT 文案 + 行内校验横幅、gui/test/volume/ fmeshValidation.test.ts 新增 24 + fmeshState.test.ts 迁移 +3，vitest 145/tsc/build 全绿——见 §8 变更日志；④ **FMESH 傻瓜友好改造 + factor 字段**：简单/高级模式（simpleModeVisibleFields 纯函数）+ ⚡按几何自动填充（surfacesAABB.ts computeSurfacesAABB 纯函数，useDeck 接 deck.surfaces/tr_cards）+ 粒子说明/三步引导 + factor 字段全链路（FACTOR= 关键字/默认 1/正整数校验），vitest 190/tsc/build 全绿——见 §8 变更日志；另有 PM 在途改动；PROJECT_MEMORY.md / README.md 亦已在工作树改动）
- **已完成功能**：
  - 8 标签页表单编辑（基本/材料/几何/源/计数/高级/输出）
  - INP 生成/导入（含拖拽）、工作区自动保存/恢复、4 套主题
  - 材料库（50+ 预设 + xsdir 校验）、材料下拉自动填充密度
  - 3D 预览（FreeCAD CSG，`#n` 栅元补集支持）+ 平面截面（STL numpy 切）+ STEP/GEOUNED 导入
  - 条件编译行（#ifdef/#else/#endif）、全行拖拽排序
  - E0/En/T0/Tn 网格（线性/对数/自定义）、三种源模式（固定/SDEF/KCODE）、SSW/SSR 面源
  - **文本↔表单双向互转**：材料/几何/计数三标签页（深模块 `useSectionTextMode` + `/api/section-to-text` + `/api/text-to-section`）
  - MCNP 检测与一键运行、输出文件解析绘图、内联参考文档
- **待开发功能**：**网格计数（FMESH/TMESH）3D 体积可视化**（契约 docs/contracts/meshtal-visualization.md 已锁定，2026-08-14；交付版本 1.7.0）。**后端施工已完成**（2026-08-14：FMESH/TMESH 结构化五步 + app/meshtal/ 8 模块 + 3 端点 25→28 + _err hint；红基线 63 红全绿，全量 pytest **409 通过 / 0 失败**，343 基线零回归；colormap midpoints 经 PM 仲裁补正为 golden 一致）。**前端施工已完成**（2026-08-14：gui/src/volume/ 11 模块 + gui/test/volume/ 8 文件 57 用例 + windows.ts 桥 + main.rs open_volume3d_window + #/volume 路由 + TallyTab 嵌 FMeshForm + api.ts 三端点封装 + A1.1 run-mcnp 后自动 meshtal-detect；vitest **76 全绿**（19 基线不破 + 新增 57）+ tsc EXIT 0 + build EXIT 0 + golden sha256 命中）；**tester 最终复审通过（2026-08-14）**：全量 pytest **430/0** + vitest 76 + tsc/build EXIT 0 + 2 项严重闭环 + spec 打包就绪 + KPI 实测达标，仅 `#/volume` 真浏览器冒烟待人工补跑（不阻塞）；其余见"工作计划"待用户定夺
- **已知阻塞**：无（测试缺口见第 6 节"踩坑"）


## 3. 项目目录结构速查（AI 定位代码用）

> **用途**：当需要修改某个功能时，先查此表定位文件位置，避免全盘扫描。

| 目录路径 | 功能说明 | 涉及 Agent |
| :--- | :--- | :--- |
| **Python 核心引擎（app/）** | | |
| `app/models.py` | 数据模型：DeckData/CellData/MaterialData/SourceData/BasicSettings/TallySettings/AdvancedSettings 等 dataclass | 后端 |
| `app/generator/inp_generator.py` | INP 生成主引擎（**已知技术债集中地，见第 6 节**） | 后端 |
| `app/generator/inp_parser.py` | INP 解析入口 | 后端 |
| `app/generator/parsers/{core,lines,sections,validator}.py` | 解析管线（行/分段/校验） | 后端 |
| `app/generator/validator.py` | 校验逻辑 | 后端 |
| `app/freecad_preview.py` / `_freecad_csg_worker.py` | FreeCAD 3D CSG 求值（子进程） | 后端 |
| `app/stl_cross_section.py` / `_freecad_cross_section_worker.py` | 截面（numpy 切 STL，不依赖 FreeCAD） | 后端 |
| `app/step_importer_geouned.py` / `geouned_worker.py` | GEOUNED STEP→MCNP 转换封装 | 后端 |
| `app/step_importer.py` / `freecad_locator.py` | STEP 导入 / FreeCAD 定位唯一入口 | 后端 |
| `app/xsdir_db.py` / `material_presets.py` | xsdir 截面数据库 / 预设材料库 | 后端 |
| | | |
| **前端（gui/src/）** | | |
| `gui/src/App.tsx` | 主界面（顶栏/导入/生成/保存恢复/主题） | 前端 |
| `gui/src/components/` | 标签页组件：BasicSettings/MaterialTab/GeometryTab/SourceTab/TallyTab/AdvancedTab/OutputTab | 前端 |
| `gui/src/components/Preview3D.tsx` / `Preview3DWindow.tsx` | Three.js 3D 预览（独立窗口） | 前端 |
| `gui/src/components/CrossSectionView.tsx` / `CrossSectionWindow.tsx` | 平面截面（独立窗口） | 前端 |
| `gui/src/utils/DeckContext.tsx` | **单一权威表单状态**（localStorage 键 `mcnp_workspace_v1`） | 前端 |
| `gui/src/utils/useSectionTextMode.ts` | **文本↔表单互转深模块**（材料/几何/计数共用） | 前端 |
| `gui/src/utils/sectionConvert.ts` | section-to-text / text-to-section API 封装（前端契约层） | 前端 |
| `gui/src/utils/gridState.ts` | E0/En/T0/Tn 网格解析/序列化深模块 | 前端 |
| `gui/src/utils/backend.ts` | 后端生命周期（sidecar 拉起/关闭） | 前端 |
| `gui/src/utils/dataCollector.ts` | 表单→DeckData 收集 | 前端 |
| `gui/src/utils/contract.ts` | 前端数据类型定义 | 前端 |
| | | |
| **后端（gui/backend/）** | | |
| `gui/backend/api_server.py` | **HTTP 后端总入口**（路由表见第 4 节，端口 5001） | 后端 |
| `gui/backend/mcnp_bridge.py` | 打包后 sidecar 启动器 | 后端 |
| `gui/backend/generate_step.py` | STEP 生成（备用） | 后端 |
| | | |
| **窗口外壳（gui/src-tauri/）** | | |
| `gui/src-tauri/tauri.conf.json` | 无边框窗口、sidecar 配置（externalBin: python） | 后端 |
| `gui/src-tauri/src/main.rs` | Tauri Rust 入口（close_window 命令等） | 后端 |
| | | |
| **文档** | | |
| `README.md` | 项目总览（技术栈/功能/打包说明/项目结构） | — |
| `app/docs/` | MCNP 参考文档（曲面卡/FN 卡/输出卡/PRINT/C810/源分布/sample_format） | — |
| `app/UI_ARCHITECTURE.md` | UI 架构说明：三层边界/启动链路/import-root/deck JSON 契约/raw_overrides/往返保真/技术债地图 | 架构师 |
| `docs/contracts/api.yaml` | **OpenAPI 3.0 契约**（25 端点，每 path 带 operationId，防漂移闸门验证） | 架构师 |
| | | |
| **测试** | | |
| 无 test/ 目录 | **测试缺口**（见第 6 节） | 测试 |


## 4. 关键架构决策（ADR）

| 决策 | 理由 | 日期 |
| :--- | :--- | :--- |
| 后端用 Python 标准库 http.server，不用 Flask | 减少依赖，PyInstaller 打包 sidecar 更简单 | — |
| 前端单一权威状态在 DeckContext（localStorage 工作区） | 8 个标签页共享一份数据，避免多源状态冲突 | — |
| 文本↔表单互转按 section 维度做深模块（useSectionTextMode） | 材料/几何/计数三处共用一套进出文本模式逻辑，接口只暴露"进/出 + 回填回调" | 近期 |
| 3D 预览用 FreeCAD 子进程 CSG 求值输出 STL | FreeCAD 精确几何；STL 保留在会话中供截面复用（numpy 切），不重复调 FreeCAD | — |
| 截面不依赖 FreeCAD，直接 numpy 切 STL | 独立窗口即时响应，无需 CAD 内核 | — |
| GEOUNED 随程序 vendor 打包，用户只装 FreeCAD | STEP 导入开箱即用；FreeCAD 经 locator 检测/手动指定 | — |
| 全部代码遵循**深模块原则**（用户全局记忆 codebase-design-always） | 小接口覆盖复杂行为，AI 友好、可测试 | — |
| **P1 多源/分布 SDEF 表示统一**（`SDEF_FIELD_SPECS` 表驱动 + 字段序统一为 POS 首位 + D-index 由 `dist_params` 位置决定、与发射序解耦） | kitchen-sink R1/R4 字节不动点要求多源生成与分布回放产出逐字节一致；消除 §0.5.5 复合根因 #2-#5 + SI 值空格归一化 | 2026-08-12 |
| **P1 分布注释作为生成器横幅词汇**（`multi_source_comment_banner(n)` 进 `is_generator_banner`，parse 拦截丢弃、回放重发） | 复用 F-A 方案 C 的词汇冻结机制，注释不漂移不重复 | 2026-08-12 |
| **P1 raw_overrides 收敛为 `_apply_raw_override` 助手**（1145 raw_tally 门控保留"判 tally key"语义） | 消除 8 处复制粘贴；门控判 tally key 是既有行为，非 bug | 2026-08-12 |
| **3D 预览 deck 指纹缓存**（`app/preview_cache.py`，LRU 上限 3，命中跳过 FreeCAD 子进程） | 打开卡真凶=每次全量重建 3.30s；同 deck 二次打开缓存命中 ≤1s，对 API 契约透明 | 2026-08-12 |
| **前端 3D 拆深模块**：`TickGrid`（刻度生命周期+dispose 台账）/`renderGate`（dirty 按需渲染）/`cellMaterial`（默认 opaque）/`computeCameraParams`（几何归一化 + far/near ≤1e4） | 交互卡真凶=每帧 160 对象/81 纹理泄漏+无条件渲染+透明 overdraw；大坐标深度 17.5M 超 2^24；深模块接口小实现深，vitest 可测 | 2026-08-12 |
| **前端后端地址收敛 `127.0.0.1:5001`**（`gui/src/utils/api.ts` 单一常量），后端绑定 `0.0.0.0` 不动 | server 只绑 IPv4 + 前端 localhost → Chrome Happy Eyeballs 每请求 300-500ms，影响全部 25 端点；127.0.0.1 直连命中 IPv4，CORS `*` 已覆盖 | 2026-08-12 |
| **重合栅元几何检查走方案 A（FreeCAD 精确布尔 + AABB 预过滤）**，独立 `/api/check-overlap` 端点（25→26，闸门双向一致）；纯分类逻辑下沉 `app/overlap_classify.py`（stdlib 可测 seam）；检测复用 worker 已建实体、结果以 `overlaps.json` 同指纹落 preview_cache；补集 `#n` 靠精确布尔零体积天然不误报；severity=info/warning/error | 反馈 #7（参考 VISED，P2）；选 A 弃 B（STL 不封闭/漏微重叠）与 C（AABB 伪报率高）；独立端点不污染 preview-3d 性能契约；本轮不施工仅存档（契约 docs/contracts/geometry-check.md） | 2026-08-13 |
| **MCNP 卡类型唯一权威源=官方 C810.pdf**（`D:\MCNP\MCNP6\C810.pdf`）；`docs/contracts/card-lexicon.md`（词条目录+解析器清单+差异表，`docs/card-lexicon-diff.md` 为施工契约）与 `app/docs/` 蒸馏 md 均为**派生**，须随 PDF 更新；新增/改卡先回查 C810.pdf 再同步 lexicon + md | 反馈 #1 FM 漏识别暴露系统性缺陷=解析器卡类型清单未与知识库对齐；lexicon 作防漂移长期机制（登记→施工→闸门）；PyMCNP `src/pymcnp/inp/`（~230 卡类）作交叉参考（非权威，仅枚举候选 D-09） | 2026-08-13 |
| **网格计数（FMESH/TMESH）3D 体积可视化契约**（`docs/contracts/meshtal-visualization.md`，16 节）：FMESH+TMESH 都进 v1、几何外壳像 3D 预览可交互（勾选栅元+粒子）、box-average 均值降采样、128³ 默认/256³ 显式/GPU 驻留 ≤256MB/超预算弹窗；模块化 `app/meshtal/` + `gui/src/volume/`；3 端点 25→28 | 对标 VISED；用户拷问敲定全部澄清项；交付版本目标 1.7.0；测试先行 + 零新依赖红线 | 2026-08-14 |
| **meshtal-texture 返回标量帧 Uint8，前端 colorize CPU 上色**（后端不返回 RGBA）；配色 python/TS 双端 golden sha256 对照 | 改色阶上下限/阈值只重跑本地 colorize 不重取纹理；Uint8+CPU 上色避开 float 纹理扩展坑；双端 golden 防双端漂移 | 2026-08-14 |
| **体积窗口独立场景，不改 Preview3D.tsx 主组件**（复用纯模块 computeCameraParams/renderGate/cellMaterial/STLLoader）；几何外壳与体积盒统一归一化共享 offset 对齐（契约 §7.3 四断言）；体积窗口关闭不清 preview-3d 的 STL 会话 | 光线步进需要全屏 quad 与 STL 网格管线差异大；复用主组件会污染 preview3d-performance 性能契约（geometry-check ADR 先例：独立端点不污染 preview-3d 契约） | 2026-08-14 |
| **meshtal 后端落地细节（2026-08-14 施工定案 + PM 仲裁）**：meshtal_parser 数据行列序按实际 MCNP `[Energy] [Time] X Y Z Result RelError`（契约 §3.4 描述 `X Y Z E T` 为笔误，vendor 实核）；colormap golden = t-space 插值（t=i/(n-1)，round-half-even）；`test_weather_lut_midpoints` 期望经 PM 仲裁补正为 golden 一致 (249,116,22)；worker 模块顶只 stdlib，numpy/pymcnp 惰性；大文件解析走 _meshtal_worker 子进程 + meshtal_cache 不阻塞 5001 | golden 是 §4.3.1 跨语言防漂移强契约（PM 显式 pin），t-space 插值精确复现 golden；"Total" 汇总行跳过不归属单帧；has_energy_col=energyBins>1（单 bin 无列） | 2026-08-14 |


## 5. 核心业务规则（必读）

- **DeckData 是聚合根**：前端 DeckContext ↔ 后端 generate/parse 全走 DeckData 单对象，避免参数膨胀。
- **密度写在栅元卡（CELL）上**，材料卡（Mm）只含 ZAID+份额，不含密度。
- **栅元/材料/计数行支持判别联合**：`kind=="cell"|"raw"`（栅元）、`kind=="nuclide"|"raw"`（材料）——`raw` 行承载 `#ifdef/#else/#endif` 原样条件行。
- **文本模式状态存在 deck.textMode[section] + deck.rawOverrides[section]**；进文本模式前必须由后端先生成当前表单的文本（section-to-text），防数据丢失。
- **STL 会话**：3D 预览生成的 STL 保留在 `_STL_SESSION`，供截面复用；只在关预览窗口/清空时 `/api/clear-stl` 删除。
- **曲面文本解析**：GEOUNED 常见 `*TRn` 后缀或 `100*` 前缀的 TR 引用，均需提取 transform；P 卡 `A B C D` 系数形式需转三点定义（注意法向同向性）。
- **SDEF 三种模式**：`fixed`（固定点源）/ `distribution`（SDEF 分布源，SI/SP/DS 结构化 JSON 优先于 sdef_raw_text）/ `kcode`（KCODE/KSRC/HSRC）。
- **前端契约层**：`sectionConvert.ts` 只认 `{status:"ok"}` 成功响应，`/api/text-to-section` 返回 `{data}`，`/api/section-to-text` 返回 `{text}`。


## 6. 踩坑与排雷指南

- **meshtal-parse 元数据缓存缺口（2026-08-14 tester 验收发现，待后端收口）**：`app/meshtal/_meshtal_worker.py` `_mode_parse` 只 `cache.put` 从不 `cache.get`，重复 `POST /api/meshtal-parse`（同文件同 mtime）每次都全量重解析（valid_39 3.36MB 二次均 ~3.07s），契约 §4.4「meshtal-parse 元数据也缓存（防弹窗流程重复解析）」与 §8「元数据 <1s（缓存命中）」未达成。`meshtal-texture` 已缓存命中（0.2s），核心开窗链路不受影响。修法：`_mode_parse` 对每 tally 先 cache.get 命中即从缓存重建元数据，miss 才解析。
- **契约 §9.1 三测试文件缺失（2026-08-14 tester 验收发现，待补）**：`tests/unit/test_meshtal_cache.py` / `test_meshtal_worker.py` / `test_fmesh_parser.py` 未落地（落地 7/10 个）。worker 模块顶 stdlib-only 的 AST 断言（§4.5 显式要求）与 cache 单测缺位，属回归防护缺口；fmesh 往返已由 test_regress_fmesh_import 间接覆盖。实现本身合规（worker 实测仅 stdlib）。**✅ 已闭环（2026-08-14 最终复审）**：3 文件补齐（9/5/7 用例）全绿，含 `test_worker_texture_bad_tally_errors` 与 `test_multi_interval_imesh_preserved`。
- **测试笔误陷阱（2026-08-14 tester 最终复审实测）**：① **valid_39.meshtal 的 tally number 是 4 不是 1**——写 meshtal-texture 冒烟/联调脚本时 tallyNumber 须取自 meshtal-parse 响应的 `tallies[].number`（契约 §3.3 示例恰为 4）；传错 tally → 500 + hint（F4 守卫正常行为，非 bug）。② **preview-3d 单栅元 material="0" 是 void** → handler `include_void=False` 跳过 → 空 stl_files（`message="没有可预览的栅元（含 void）"`）；构造冒烟 deck 须用非 0 material（如 `"1"`）。
- **已知技术债（来自 `app/generator/review_findings.json`，7 项，全部在 inp_generator.py）——P1 已全部清偿（2026-08-12）**：
  0. **D-03 类边界缺陷（2026-08-13 tester 验收新发现，归词条审计 D-03 既有类）**：紧凑 INP（节间无空行分隔、且无 MODE/NPS/SDEF 等数据关键词先行）下，split_sections（sections.py DATA_PATTERNS）不识别 `^[*+]?F\d+`（带 * / + 前缀）与 `F\d+[XYZ]`（F5X/Y/Z 环探测器）→ 整卡误分曲面段被 parse_surfaces 丢弃。真实样例 photon_skyshine.inp（有标准空行结构）与生成器产出不受影响；#1/#6 修复未引入（sections.py 仅新增 `^FM\d+$`），与 #2 前缀保留正交。修复建议（P1，排期时）：DATA_PATTERNS 补 `^[*+]?F\d+` 与 `^[*+]?F\d+[XYZ]:?`（对照 card-lexicon-diff.md D-03）。
  0. **`_entry_gate_matches` 自参照正则（2026-08-13 tester 验收观察）**：test_regress_fm5_import.py 的 `test_entry_gate_recognizes_fm5` 在测试内逐字复刻入口门正则，若 core.py 漂移则单测不捕获；但同文件 `test_fm5_absorbed_into_tally_and_generator_replays` 走真实 parse_data_cards 守卫实际门，FM 修复整体有护栏。
  1. ✅ `raw_overrides` 覆盖守卫 8 处复制粘贴 → 收敛为 `_apply_raw_override`（commit 1488aae），raw_tally 门控判 tally key 语义保留
  2. ✅ `_generate_en_cards` 函数内重复 `import re`（P0 期间已 Resolved）
  3. ✅ 函数内 `import json as _json` + `import sys` + `[E0DBG]` 调试 print → 清理（commit c774e56），grep 归零
  4. ✅ `_generate_single_source` 两段相同 SDEF 构造 → 合并 `_build_sdef_parts`（commit 52ca251），字节不变
  5. ✅ `_generate_multi_source` 145 行 5 子关注点 → 拆 5 函数（commit e404172），概率归一可单测
  6. ✅ 多源源字段名 3 处枚举 → `SDEF_FIELD_SPECS` 表驱动（commit e404172 + 018ced5）
  7. ✅ 函数内 `from pymcnp import inp` → 提到模块顶部（commit bf0a2c7），导入期 fail-fast
- **测试缺口**：全仓库无 test/ 目录、无 pytest/unittest 用例。对生成器/解析器这种核心引擎是高风险。
- **P0 测试防线（2026-08-12 建立）**：`tests/` 目录，`python -m pytest tests/ -v`。总体 234 通过 / 17 失败（17 个全为技术债/往返缺陷的"按设计先红" pin，红=标记缺陷存在，见 `docs/qa-report.md`）。
- **R1 不动点（正确性红线）不成立 — 阻塞 M1.4**：`generate(parse(generate(d))) != generate(d)`。根因是生成器 C 注释头泄漏（`C  Cell Cards: N cells defined` → 变成栅元 `$` 注释；`C  Surface Cards: N surfaces defined` → 计入曲面文本；`C  ===== Data Cards ====="` → 捕获进 other_cards），输出逐代膨胀。P1 动引擎代码前必须先修。
- **validate_deck 与 CellRow 不兼容**：`app/generator/validator.py:validate_all` 直接访问 `cell.surface_expr`，而 `deck.cells` 是 `CellRow`（kind/cell/text 嵌套）→ 对含栅元 deck 抛 AttributeError。生产 `/api/validate-inp` 走文本层 `parsers/validator.validate_inp_text`，不受影响。
- **多粒子计数卡 round-trip 丢失**：生成输出 `F4:N,P`（逗号合并），`parse_f_tally` 只认单粒子设计符 → 多粒子计数 parse 后消失（进 other_cards）。
- **材料 options 丢失**：MaterialData.options（nlib= 等）生成时追加到材料末行尾，被解析器并入 raw 行 → round-trip 后 options 为空。
- **`_wrap_long_lines` 的 & 续行符污染字段**：超 80 列行拆分附加 `&`，解析后污染 surface_expr/vec 等字段（R2 已对尾 & 容差）。
- **ksrc_points 数值坐标崩溃**：`_generate_kcode` 对数值坐标（int）`.strip()` 崩溃；契约要求字符串坐标（前端发字符串，属潜在缺陷）。
- **parse_sdef_simple 的 EFF 裸值静默丢弃**：EFF 在 _KNOWN_KEYS 但 _apply_sdef_param 无 EFF 分支 → 数据丢失点。
- **UI_ARCHITECTURE.md 已重建**（2026-08-12）：后端 F-A~F-E 重新施工后原 P2 文档因未 commit 被回滚误清空，架构师已补建 7 节文档并用 Grep 全量重锚定行号（见 §8 变更日志）。
- **契约文档已补**（2026-08-12）：`docs/contracts/api.yaml` 覆盖 api_server.py 全部 25 端点（每 path 有 operationId）；漂移闸门 `tests/integration/test_api_contract.py` 会 AST 断言 handlers 字典每 path 在 api.yaml 有 operationId。
- **Cargo.toml 版本隐患（2026-08-14）**：`gui\src-tauri\Cargo.toml` version 停在 **1.6.3**，v1.6.4 打包时漏改（手动打包文档当时只写"两处"）。Tauri 以 tauri.conf.json 为权威不影响打包，但不一致是隐患 → 手动打包方法步骤 1 已改"三处+README"，打包 1.7.0 时 Cargo.toml 必须同步。
- **打包注意**：Tauri build 需要 `RUSTUP_HOME/CARGO_HOME` 指向 D:\rust；sidecar 用 PyInstaller（spec：`gui/mcnp_sidecar.spec`，产物名 "python"）；后端窗口关闭时经 Rust `close_window` 命令一起退出。


## 7. 技术争议与决议

| 争议点 | 方案 A | 方案 B | 最终裁决 | 裁决理由 |
| :--- | :--- | :--- | :--- | :--- |
| F-A R1 不动点：生成器 C 注释头泄漏，解析器吸收 vs 生成器改头 | 解析器吸收防护（仅节头词汇精确剥离） | 生成器改头为不可吸收形式 | **方案 C，以 A 为主、B 为辅**（2026-08-12） | 方案 B 单独不可行：MCNP 注释只有 C 一种形式，现有解析器对任意 C 行都会在栅元注释/曲面 verbatim/other_cards 三路吞掉，不存在合法且三阶段天然惰性的注释形式；曲面段 verbatim 回放使生成器单侧无法根治。方案 C 把节头冻结为 banners.py 单一事实来源，生成器与解析器共享，R1 测试为漂移兜底；用户可见 INP 输出风格保留 |


## 8. 最近变更日志

| 日期 | 变更类型 | 改动描述 | 涉及 Agent |
| :--- | :--- | :--- | :--- |
| 2026-08-14 | 前端 | **FMESH 傻瓜友好改造 + factor 字段（测试先行 35 新用例，对齐后端 factor 契约）**：① 简单/高级模式——默认「简单」只露核心 4 项（粒子 + IMESH/JMESH/KMESH），其余（ORIGIN/INTS×3/能量/时间/MAT/OUT/AXS/VEC/TR/factor/GEOM）折叠进「高级模式 ▾」；抽纯函数 `simpleModeVisibleFields` + `FMESH_SIMPLE_FIELDS`/`FMESH_ADVANCED_FIELDS`，展开状态存组件本地 state（不落 deck）；② ⚡按几何自动填充——新增深模块 `gui/src/volume/surfacesAABB.ts` `computeSurfacesAABB(surfaces[, trCards])→{min,max}|null`（解析平面/球/圆柱/宏体，GQ/SQ/P/K* 跳过，TR 有卡则平移/旋转、无卡则跳过容错，任一轴无界返回 null）+ `aabbToFmeshValues`；FMeshForm 用 useDeck() 接回 deck.surfaces/tr_cards，按钮一键填 ORIGIN+IMESH/JMESH/KMESH（网格覆盖模型），解不出 alert；③ 粒子说明「每个网格计数一个粒子（N/P/E）；要多种粒子就加多行」+ 三步引导「①选粒子 ②点自动填充 ③解析看 3D 结果」；④ factor 字段——`FmeshRow.factor` 默认 "1"（高级模式）、`KEY_TO_FIELD` 加 `FACTOR→factor`、卡体生成发 `FACTOR=`（非空才发）、解析/载荷/回读透传（JSON key= factor）、`validateFmeshRow` 加正整数校验（非法友好提示）；`hasStructured` 未含 factor（raw 兜底不破）。**vitest 190 全绿（155 基线不破 + 新增 35：fmeshFactor 9 + simpleMode 4 + surfacesAABB 22）+ tsc/build EXIT 0**；零新依赖、未改后端（app/、gui/backend 零触碰；后端 factor 由 backend-fmesh-factor 并行同步）。改动清单 docs/frontend-changes.md 追加节。未 commit | 前端 |
| 2026-08-14 | 后端 | **FMESH 表单改进（字段对齐 MCNP6，测试先行 5 新用例）**：① `FmeshDefinition` `eints→emints`、`t_ints→tmints`（models.py + fmesh_parser `_KEYS` + api 序列化 JSON key + 卡体生成关键字 `EMINTS`/`TMINTS`）；**解析容错** `EINTS`/`EMINTS`、`TINTS`/`TMINTS` 两种拼写导入都映射到 emints/tmints；② 新增 `axs`/`vec`/`tr` 字段（解析认 `AXS=`/`VEC=`/`TR=`，回放保留；GEOM=CYL 时前端显示、后端只管存/回放）；③ `out` 确认已有并回放 `OUT=`；④ GEOM 值**连写单 token**（`(fd.geom or "").strip().split()[0]`，避免 `GEOM=X Y Z` 非法）；⑤ **修复既有缺陷** `_is_fmesh_body_line`（core.py:900）——5 空格缩进的 `TMESH=`（FMESH 时间关键字）被负向前瞻 `(?![A-Za-z0-9=])` 修正不再误判为 TMESH 卡头（此前 tmesh/tmints deck 级 round-trip 必丢）；⑥ api_server `_fmesh_from_list` 读新字段 + 向后兼容回退旧 `eints`/`t_ints`；⑦ **TMESH/RMESH/CMESH 路径零改动**（后端代码路径保留，UI 隐藏是前端事）。**字段契约最终清单**见 docs/backend-changes.md §K.1（emints/tmints/axs/vec/tr/out + GEOM 连写，供 PM 转前端对齐）。全量 pytest **445 通过 / 0 失败**（440 基线零回归 + 5 新增）；R1 不动点不回归；api.yaml 无 fmesh_defs schema → **无变更**。改动清单 docs/backend-changes.md §K | 后端 |
| 2026-08-14 | 前端 | **FMESH 表单改进前端落地（对齐后端 §K 契约，测试先行）**：① 字段改名 `eints→emints`/`t_ints→tmints`（fmeshState FmeshRow/empty/KEY_TO_FIELD/buildFmeshPayload/fmeshDefsToRows；生成发 `EMINTS=`/`TMINTS=`，导入容错 EINTS/EMINTS、TINTS/TMINTS）；② 新增 `axs`/`vec`/`tr`（解析/回放/载荷透传）；③ GEOM 四选下拉 XYZ(默认)/REC/CYL/RZT + `normalizeGeom`（大写单 token 连写）+ `isCylGeom`；④ AXS/VEC 仅 cyl 系显示 + `vectorsParallel` 平行校验；⑤ TR 可选变换编号（正整数）；⑥ OUT 九选下拉 COL(默认)/CF/COLSC/CFSC/IJ/IK/JK/NONE/XDMF（CF 体积、NONE 不打印、XDMF 供 ParaView）；⑦ MAT 帮助文案（0=粒子所在格材料默认，非 0=指定材料）；⑧ 校验抽纯函数 `validateFmeshRow`（正整数/条目数匹配/单调递增+ORIGIN/cyl kmesh 末值=1/平行/TR/大网格 >128³ 内存警告），表单行内友好提示，TMESH 只读行跳过；另修 `fmeshKindControl` 判 cyl 改 `isCylGeom`（geom 现大写单 token）。**vitest 145 全绿（118 基线不破 + 新增 27：fmeshValidation 24 + fmeshState +3）+ tsc/build EXIT 0**；零新依赖、未改后端。改动清单 docs/frontend-changes.md 追加节。未 commit | 前端 |
| 2026-08-14 | 后端 | **/api/expand-formula 份额语义扩展（is_weight 参数）**：`_handle_expand_formula`（api_server.py:606-650）读 `is_weight = data.get("is_weight", True)`（缺省 true 向后兼容；None→true、字符串 `""/0/false/no`→false 防御性布尔解析），调 `pymcnp.inp.M_0.from_formula({sym:1}, is_weight=..., cutoff=1e-9)` 显式透传。默认=质量份额负号、`is_weight=false`=原子份额正号（MCNP 约定 负=质量/正=原子），正负号全链路保留。api.yaml expandFormula body schema 文档化 is_weight（path/operationId 不变，漂移闸门 `test_api_contract.py` 7 用例绿）。**新增** `tests/integration/test_api_expand_formula.py` 7 用例（子进程 HTTP 范式，不 import api_server）：默认负号 / 原子正号 / 符号约定互为相反数 / 显式 true==默认逐字节一致 / null 与字符串 "false" 防御解析 / 空公式 500 error 不回归。**未动份额归一化逻辑**（abs-sum 归一行为与现状一致）。全量 pytest **440 通过 / 0 失败**（433 基线零回归 + 7 新增）。改动清单 docs/backend-changes.md §J | 后端 |
| 2026-08-14 | 后端 | **P0 打包模式 meshtal worker spawn 修复（v1.7.0 实包冒烟）**：根因 = api_server `subprocess.run([sys.executable, worker_script])` 打包版 sys.executable 是冻结 exe → 传参运行冻结入口再启第二个 5001 挂起。修法：① `gui/backend/mcnp_bridge.py` 加 `--meshtal-worker` argv 分派（→ `meshtal._meshtal_worker.main()`，stdin JSON→stdout JSON，不 import api_server/不启 HTTP，`_MEIPASS/app` 路径兜底）；② api_server `_meshtal_worker_script()` → `_meshtal_worker_cmd()`（frozen `[sys.executable, "--meshtal-worker"]` / dev `[sys.executable, mcnp_bridge.py, "--meshtal-worker"]`），parse/texture 两处 handler 改调；③ `gui/mcnp_sidecar.spec` `_hidden` 补 meshtal 9 模块进 PYZ（_keep_dirs data 保留核对）；④ 新增 3 条 dev spawn 测试（python mcnp_bridge.py --meshtal-worker 喂 stdin 断言 stdout 信封：parse grid_bounds / texture 标量帧 / 坏 tally error）。全量 pytest **433 通过 / 0 失败**（430 + 3 新测试，343 基线零回归），dev spawn 实测走通。改动清单 docs/backend-changes.md §I | 后端 |
| 2026-08-14 | 测试 | **网格计数可视化·最终复审（施工链最后一道验收：通过）**：独立复跑全量 pytest **430/0**（14.08s，343 基线零回归 + 66 meshtal + 21 三新文件）+ vitest **76/13 文件** + tsc/build EXIT 0 + 漂移闸门 25→28 双向绿（handlers dict 28 + api.yaml 28）+ R1 不回归。**2 项严重闭环**：① parse 元数据缓存（`_mode_parse` 先 `get_manifest` 命中即返回，实测 valid_39 HTTP 二次 parse **0.233s** <1s，独立 worker 子进程 0.2314s）；② §9.1 三测试文件（test_meshtal_cache 9 / test_meshtal_worker 5 / test_fmesh_parser 7，含 `test_worker_texture_bad_tally_errors`/`test_multi_interval_imesh_preserved`）全绿。**P0 spec 补丁生效**：spec `_keep_dirs` 含 `"meshtal"` + api_server `_meshtal_worker_script()`（MEIPASS 感知，dev 返回 PROJECT_DIR 路径，parse/texture 两处调用）。**KPI 实测（真实 5001+FreeCAD）**：preview-3d HIT **0.011s** ≤1s、meshtal-texture **0.402–0.582s** <2s、parse 缓存 **0.233s**、大文件解析期并发探活 **3.6–17.2ms** 不阻塞。F1-F5 全过、纪律零违例、**打包就绪**。**授权删除执行**：`gui/test_electron.cjs` 已 `git rm`（用户 2026-08-14 具名授权，本次唯一写操作）。💡 `#/volume` 真浏览器冒烟记录待人工补跑（不阻塞）。qa-report.md 〇-7 节已更新 | 测试 |
| 2026-08-14 | 后端 | **网格计数后端复工收尾（QA 2 项严重 + spec 打包前置）**：① `_meshtal_worker._run` try/except 从 main 移到 _run（error 信封，坏 tally 不直抛 KeyError，直接调用与 stdin 协议都返回 error）；② fmesh 多区间 IINTS 定位为**测试笔误**（JMESH 行误写 `IINTS=` 重复键覆盖首值，解析器本身正确）→ 修正测试卡体为 `JMESH=10 20 JINTS=2 2`；③ `gui/mcnp_sidecar.spec` `_keep_dirs` 加 `"meshtal"` + api_server 新增 `_meshtal_worker_script()`（sys._MEIPASS 感知照 step_importer_geouned:22）→ app/meshtal 8 模块落盘 `_internal/app/meshtal/`（v1.7.0 打包前置）；④ 删 api_server PYVISTA_OFF_SCREEN 死配置（pyvista 0 import）。**⛔ 删 gui/test_electron.cjs 被权限系统拦截**（既有文件删除需人工用户具名授权，PM 请求不构成用户同意，待用户指令或用户自行删除）。新增 3 测试文件（test_meshtal_cache 9 / test_meshtal_worker 5 / test_fmesh_parser 7 = 21 用例）。全量 pytest **430 通过 / 0 失败**（343 基线零回归 + 全部新增含 3 新文件全绿）；R1 不动点不回归。改动清单 docs/backend-changes.md §H | 后端 |
| 2026-08-14 | 测试 | **网格计数可视化验收复核（契约步 5：有条件通过）**：独立复跑全量 pytest **409/0**（16.49s，343 基线零回归 + 66 新增）+ vitest **76/13 文件**（19 基线不破 + 57 新增）+ tsc/build EXIT 0 + 漂移闸门 7/7 + 红基线 63 红→63 绿（3 绿对照保持）+ R1 不动点不回归 + §7.3 对齐断言 5 用例绿（alignWorld 抽查确为单一归一化 offset）。KPI 实测：preview-3d HIT **0.041s**（MISS 2.491s）、meshtal-texture **0.202s**（缓存命中）、大文件解析期 5001 并发响应 2–25ms（worker 不阻塞）、128³ 切帧 <50ms（vitest）、golden sha256 跨语言逐字节一致（python 实算 `36770ae2…` 命中）。F1-F5 逐条核对全过；纪律零违例（零新依赖/测试不 import 禁项/未改 Preview3D.tsx/ResultWindow 关闭不清 STL/零测试改动）。**2 个 ⚠️ 严重待收口**：① meshtal-parse 元数据缓存未实现（_meshtal_worker `_mode_parse` 从不 cache.get，只 put；重复 parse 全量重解析，valid_39 二次 3.07s，契约 §4.4/§8「元数据 <1s 缓存命中」未达成，texture 缓存正常 0.2s 不受影响）；② 契约 §9.1 三测试文件缺失（test_meshtal_cache.py / test_meshtal_worker.py / test_fmesh_parser.py，worker AST stdlib-only 断言与 cache 单测缺位，fmesh 往返已间接覆盖）。无 ❌ 致命；建议后端补齐两处 + 人工补跑 `#/volume` 真浏览器冒烟后转通过。qa-report.md 〇-6 节 + 踩坑指南更新 | 测试 |
| 2026-08-14 | 前端 | **网格计数可视化前端施工完成（meshtal-volume）**：新建 `gui/src/volume/` 11 模块（volumeShader WebGL2 光线步进 RawShaderMaterial 自带 #version 300 es / VolumeRenderer Data3DTexture+统一归一化对齐+A2.1 自动取景 / colorize golden 命中 `36770ae2…`（round-half-even）+阈值 alpha0+默认 range=scalarRange / alignWorld §7.3 四断言 / downsampleRequest F3 弹窗「要更流畅，还是要更精细？」/ fmeshState 卡体↔结构化 / workflow F1 空态三步+可操作提示 / ColorLegend F5.2 单位+上下限 / FMeshForm 幽灵文字 F5.1+解析启动器 / VolumeControlPanel 高级控件折叠 F2 / ResultWindow 关闭只 close_window 不清 STL 会话 §14）+ `gui/test/volume/` 8 文件 57 用例（含 §7.3 对齐断言、golden sha256、128³ 计时 <50ms、A2.1 自动取景）。接线：windows.ts KEY_VOLUME3D/openVolume3D/readVolumeData + main.rs open_volume3d_window（label `volume3d`）+ App.tsx `#/volume` 路由 + generate 载荷 `tally.fmesh_defs` + TallyTab 嵌 FMeshForm + api.ts meshtal 三端点封装（errorHint F4）+ A1.1 PreviewDialog run-mcnp 后自动 meshtal-detect。**vitest 76 全绿（19 基线不破 + 57 新增）+ tsc EXIT 0 + build EXIT 0**。偏差：three 0.160 类名 Data3DTexture（DataTexture3D 旧名）；GridEditor 按契约 §4.7.1 不复用（卡语义不同）。改动清单 docs/frontend-changes.md | 前端 |
| 2026-08-14 | 后端 | **网格计数可视化后端施工完成（meshtal-volume）**：① FMESH/TMESH 结构化五步走（sections.py 补 `^TMESH`/`RMESHn`/`CMESHn` + core.py 入口门 FMESH/TMESH 分支吸收 fmesh_defs + 新建 app/meshtal/fmesh_parser.py + models.py `FmeshDefinition`/`TallySettings.fmesh_defs` + inp_generator `_generate_tallies` 后回放 + api_server `_tally_from_dict` 读 fmesh_defs）；② 新建 app/meshtal/ 8 模块（meshtal_parser pymcnp 优先+轻量兜底 / volume_builder box-average 降采样+归一化后标量 / colormap 天气图 golden / downsample_plan 预算 / meshtal_cache pickle / deck_match AABB/overlap/offset / _meshtal_worker 子进程 stdlib-only）；③ 3 端点（meshtal-detect/parse/texture，25→28 漂移闸门双向一致）+ `_err(msg, status, hint="")` 加性兼容 + api.yaml 同步；④ 红基线 63 红→**全绿**（全量 pytest **409 通过 / 0 失败**，343 基线零回归 + 66 新增全绿）。**PM 仲裁定案**：`test_weather_lut_midpoints` 期望 `(249,115,22)` 系 tester 目测（#F97316 本身 R249 G115 B22），未按 t-space 插值实算，与 golden（t-space 锁定 lut[191]=(249,116,22)）矛盾 → 补正为 `(0xF9,0x74,0x16)=(249,116,22)`，只改该期望值。**伴生正确性修复**：parse_data_cards `pending_c` C 注释 EOF 未 flush 静默丢弃 → 已 flush 进 other_cards（R1 不变量必要）。改动清单 docs/backend-changes.md §G | 后端 |
| 2026-08-14 | 管理 | **下次打包版本 = 1.7.0**（用户明确指定，非 1.6.5）；网格计数可视化大功能按 1.7.0 发布。Cargo.toml 版本隐患记录（v1.6.4 漏改停在 1.6.3）。手动打包方法步骤 1 改"三处+README"（tauri.conf.json/package.json/Cargo.toml/README 徽章）+ 踩坑 #6 + 速查 + 对照表更新。契约 meshtal-visualization.md 标注交付版本目标 1.7.0 | 项目经理 |
| 2026-08-14 | 架构师 | **网格计数（FMESH/TMESH）3D 体积可视化契约产出**：`docs/contracts/meshtal-visualization.md`（16 节）。FMESH+TMESH 都进 v1；模块化 `app/meshtal/`（7 模块）+ `gui/src/volume/`（9 模块）+ 3 端点（25→28）+ 独立窗口 `volume3d` + `#/volume` 路由。关键裁决：标量帧 Uint8 + 前端 colorize、box-average、几何外壳与体积盒共享归一化 offset 对齐、worker + pickle 缓存、不改 Preview3D.tsx、窗口关闭不清 STL 会话。交付版本目标 1.7.0。测试先行：tester 先建红基线。**尚未施工** | 架构师 |
| 2026-08-13 | 修复/后端 | **词条专项"真需修 3 条" D-03 + D-07 + D-10**：① D-03 sections.py DATA_PATTERNS（:148-185）补 F 家族前缀形态（`^[*+]?F\d+:`/`^[*+]?F\d+$`/`^[*+]?F\d+[XYZ]:?`/`F(?:IP\|IR\|IC)\d+`/`FM`/`FC`）+ 辅助卡缺口词条（Cn/DE/DF/FS/SD/CF/SF/EM/TM/CM/TF/DD/DXT/SB/DS/SC/ELPT/NOTRN/TALNP/MPLOT/RAND/FILES/IDUM/RDUM/FMESH）→ 节首直出未设计卡进 data_lines→other_cards 兜底，不再误分曲面/栅元段；② D-07 parse_sdef_simple 裸分支白名单补 ERG/WGT/CEL/TME/EFF/RAD/EXT/DIR/X/Y/Z（:593-620）+ 裸 AXS/VEC 多值分支 + `POS=` 续值遇已知 key 停靠（:485-497）；③ D-10 parse_data_cards 6 处 other_cards append 改存 raw_line（:1044/:1085/:1090/:1154/:1286/:1317）保留行内 `$ 注释`。回归测试 tester-dfix 红基线 45 红→**全绿**（D-03 29 / D-07 12 / D-10 5），全量 pytest **343 绿 / 0 红**（298 预修通过 + 45 红转绿），R1/R2-R4/契约漂移闸门/样例冒烟全绿。改动清单 docs/backend-changes.md 附录 F | 后端 |
| 2026-08-13 | 架构师 | **lexicon 补录 PDF 核验结果（arch-lexicon，收尾）**：① 权威源声明修正——"卡格式权威 = MCNP5 卷 II Ch.3（打印页 3-1~3-166），本 PDF 无 MCNP6.1 卷 II Ch.3；MCNP6 专属卡另以 MCNP6 手册卷 II Ch.3 核验（本机缺，标注待核验）"（card-lexicon.md 头部 blockquote + 权威源 bullet + §四）；② **D-12 20 族 + WWP 按 C810 页号补录为正式词条**（§1.3.8 D-12 表：AREA 3-25/EXT 3-41/VECT 3-42/PWT 3-40/VAR 3-35/TSPLT 3-38/BBREM 3-53/FCL 3-43/PDn+DXCn 3-52/DRXS 3-125/AWTAB+XSn 3-127/PIKMT 3-128/MGOPT 3-129/THTME 3-137/URAN 3-32/WWG+WWGE 3-48/MESH 3-49/WWP 3-46）；③ **D-13 标注**：ACT/BURN/PHYS:H/HE = MCNP6 专属（§1.3.1/§1.3.5/§1.3.7）、MPHYS/LCA = 存疑（C810 全文 0 出现）。D-11 三分核验表已由 tester 写入 §1.3.8，diff D-11~D-13 已同步。未改 app/gui 源码 | 架构师 |
| 2026-08-13 | 测试 | **C810.pdf 页级文本抽取 + 卡类型索引核验（tester，PyMuPDF 1.28 全库页级抽取，零安装）**：① **C810 结构鉴定**：本 PDF 实际 = MCNP5 卷 I+II 全文 + MCNP6.1/MCNP5 发布说明 + 附录 + 索引；**卡格式权威章 = MCNP5 卷 II Ch.3**（PDF 页 526-691、打印页 3-1~3-166），Table 3.11（3-161~3-164）为全卡汇总表；**权威源声明修正**——MCNP6 专属卡（BFLD/EMBED/KPERT/KSEN/DAWWG/TROPT/UNC/COSY/ACT/BURN/PHYS:H/HE 等）本 PDF 无格式定义，须另以 MCNP6 手册卷 II Ch.3 核验。② **D-09 ~35 族候选三分定案**（差异表新增 D-11）：确认真卡 27 族（AREA 3-25/ESPLT 3-36/TSPLT 3-38/PWT 3-40/EXT 3-41/VECT 3-42/FCL 3-43/WWE/WWN 3-45/WWP 3-46/WWG/WWGE 3-48/MESH 3-49/PDn/DXCn 3-52/BBREM 3-53/VAR 3-35/URAN 3-32/SPDTL 3-120/DRXS 3-125/TOTNU/NONU 3-126/AWTAB/XSn 3-127/PIKMT 3-128/MGOPT 3-129/THTME 3-137，另 ZA/ZB/ZC Appendix A/F 认可、ZD 无）；MCNP6 专属仅提及 8 族（BFLD/COSY/DAWWG/EMBED族/KPERT/KSEN/TROPT/UNC）；本 PDF 无证据 16 族（BFLCL/COSYP/DMn/FMULT/HISTP/KOPTS/LCB/LCC/LEA/LEB/OTFDB/STOP/WWGT/WWT/ZD/MXn，PyMCNP 类多空 docstring=占位）。③ **PDF-only 词条差异**（D-12）：AREA/EXT/VECT/PWT(数据卡)/VAR/TSPLT/BBREM/FCL(数据卡)/URAN/THTME/PDn/DXCn/DRXS/AWTAB/XSn/PIKMT/MGOPT/WWG/WWGE/MESH 20 族 Ch.3 真卡但词条目录正文未登记（解析器均 other_cards 兜底无丢失）。④ **词典-only 复核**（D-13）：ACT/BURN/PHYS:H/HE=MCNP6 专属；MPHYS/LCA=C810 全文 0 出现疑似占位。文档已更新 `docs/card-lexicon-diff.md`（D-11~D-13）+ `docs/contracts/card-lexicon.md`（§1.3.8 核验表）。未改任何 app/gui 源码 | 测试 |
| 2026-08-13 | 测试 | **#1/#6 修复验收 + #2 往返测试（tester 独立复核）**：①6 回归用例逐条审查——全部真守卫修复（FM5 识别/吸收/回放、占位两序吸收、cells 字段全保留、materials/tally 对照不回归），无删弱断言；`test_entry_gate_recognizes_fm5` 为测试内自参照正则，但同文件 `test_fm5_absorbed_into_tally_and_generator_replays` 走真实 parse_data_cards 兜底守卫实际入口门。②全量 pytest **297 绿 / 0 红**（287 基线 + 6 D-01 + 4 #2；D-01 由 backend-fb16 会话期间落地转绿）；R1 不动点 + 契约漂移闸门 7/7 绿。③真实 HTTP 联调通过：text-to-section cells 3 栅元字段全保留 / parse-inp FM5 multiplier 带出 / generate 回放 `FM5  8.65061E10 1 -5 -6` / 再 parse 保留；**真实样例 photon_skyshine.inp 端到端：f5z:p → F5 tally + multiplier 全保留**。④**新发现 D-03 类边界**（非 #1/#6 回归）：紧凑 INP（节间无空行）下 `*F4`/`+F4`/`F5Z:P` 被 split_sections 误分曲面段丢弃（DATA_PATTERNS 缺 `^[*+]?F\d+`/`F\d+[XYZ]`），建议 P1 排期修复，已记踩坑指南。⑤新增 tests/parser/test_regress_fm_prefix.py 4 用例（#2 *F/+F 前缀导入→回放→完整 INP 往返不丢） | 测试 |
| 2026-08-13 | 修复/后端 | **词条专项 D-01 独立 SIn/SPn 静默丢弃（P0）**：`parse_data_cards` SI/SP 分支（core.py:1125-1134，原 :1101-1102）——①最小修复：SI/SP 行 append 进 `other_cards`（保底不丢，round-trip 保真）；②增强修复：`source_mode=="surface"`（SSW/SSR 面源）时并入结构化分布（新增 `_merge_sisp_entry` core.py:158 按 id 合并 SI/SP/SB/DS 子字段，复用 `_parse_sisp_structured`，与 SDEF 分支一致；source_mode 保持 surface）。安全性：surface 生成器只走 SSW+SSR 忽略 sdef_distributions → 无双重发射；other_cards 末尾无条件回放 → round-trip 保真。回归测试 test_regress_independent_si_sp.py 6 用例 4 红 2 绿 → **6 全绿**（对照场景 source_mode=distribution + JSON id=5 不回归）。全量 pytest **293 绿 / 0 红**（287 + 6）。改动清单 docs/backend-changes.md 附录 E | 后端 |
| 2026-08-13 | 修复/后端 | **#1/#6 后端 P0 修复**：#1 FM 全链补 multiplier（入口门扩 `^[*+]?FM\d+` core.py:1030 / parse_f_tally FM 分支 + Fn 未先行建占位吸收 core.py:721-737,790-811 / TallyDefinition.multiplier models.py:200 / _generate_tallies F 卡后回放 FM 卡 inp_generator.py:644-671 / api_server 三处序列化带出 358,480,665 / sections.py DATA_PATTERNS 补 `^FM\d+$`:146）；#6 `_handle_text_to_section` cells 分支独立 shell（api_server.py:718-719）cell 文本入栅元段，字段全保留，materials/tally 分支不变。**回归测试状态**：tester 提交的 2 回归文件 pin 修复前 bug 行为（提交时即绿），已更新为断言修复后正确行为（强化非删弱）6 用例全绿。全量 pytest **287 绿 / 0 红**（271 基线 + 6 回归 + 10 #5）+ R1/R2-R4/契约漂移闸门全绿。改动清单 docs/backend-changes.md 附录 D | 后端 |
| 2026-08-13 | 架构师 | **解析器词条清单 × 知识库对齐审计（只读，上级纠正后重锚定）**：产出 `docs/contracts/card-lexicon.md`（MCNP 卡类型权威词条目录）+ `docs/card-lexicon-diff.md`（差异表+施工契约）。**唯一权威源=官方 `D:\MCNP\MCNP6\C810.pdf`**（本文件+app/docs 蒸馏 md 均为派生，已改声明）。**差异 9 条**：P0×1（D-01 独立 SIn/SPn 静默丢弃 core.py:1101-1102）；P1×3（D-02 计数辅助卡无结构化关联、D-03 sections.py 分节识别缺口、D-04 _KNOWN_OTHER_CARDS 精确匹配失效）；P2×5（D-05 FMn 前端无乘子 UI、D-06 En 参数化不解析、D-07 SDEF 裸参数白名单缺、D-08 md 主清单缺计数辅助卡=本次已补录 C810_卡片格式详细.md、D-09 PyMCNP 交叉核对 ~35 个 MCNP6 卡 md/解析器均未收录=兜底 other_cards 保留无丢失）。**PyMCNP 交叉核对**（`D:\MCNP\PyMCNP\src\pymcnp\inp\` ~230 卡类）已纳入，新增 D-09。**工具限制**：本会话 Read 缺 poppler-utils 无 shell，**未能做 C810.pdf 页级核验**（诚实声明于两文档），词条标「待 C810 核验」；FM 已修（工作树全链通，仅前端 UI 缺）。**施工契约已出，PM 审计通过（2026-08-13）**：D-01 已由 backend-fb16 修复（core.py:1124-1134，pytest 293 绿，差异表已标已修）；D-02/D-03 待 PDF 核验后统一排后端；D-04~D-07 按原计划；D-09 待 PDF 核验后登记；PDF 页级核验由 tester-pdf 尝试本机 PDF 库（红线：不自动安装） | 架构师 |
| 2026-08-13 | 修复/后端 | **反馈 #5 后端联动落地**：`_generate_materials`（inp_generator.py 205-220）options 按 `\n` 拆分——首段内联 `M{n}` 后（F-D pin 无换行字节不变），其余段作 M 卡 5 空格续行（防 MCNP 把 gas=/plib= 当新卡）；`\r\n` 归一化、空白段跳过。新增 `tests/unit/test_generator_materials_multiline_options.py`（10 用例，含 `_wrap_long_lines` 交互：超长 options 行 & 续行后全行 ≤80 且续行合法）。全量 pytest **287 绿 / 0 红**（基线 277 零回归），R1 不动点不回归。改动清单 docs/backend-changes.md §C | 后端 |
| 2026-08-13 | 文档 | **反馈 #7 几何检查可行性方案已落契约存档**：`docs/contracts/geometry-check.md`（PM 采纳方案 A，本轮不施工仅存档）。核心：AABB 粗筛 + worker 内 FreeCAD 精确布尔求交（复用 Step 3 已建实体）；补集 `#n` 靠精确布尔零体积天然不误报；容差 `max(1e-6, 1e-4*min_vol)` + `max_boolean_ops`=300 + severity=info/warning/error；独立 `/api/check-overlap`（25→26，漂移闸门双向一致）；纯分类下沉 `app/overlap_classify.py`（stdlib 可测 seam）；结果 `overlaps.json` 同指纹落 preview_cache；preview-3d 性能契约零回归。待 P0/P1 完成后排期施工 | 架构师 |
| 2026-08-13 | 前端 | 反馈 #3/#4/#5 前端落地（frontend-fb1）：TallyTab.tsx idsRef 按位置缓存 id（number 变化不再改 key，行不重挂载光标不丢，新行 number+type+particle 种子+Set 去重）；AdvancedTab.tsx 其他卡 textarea minHeight 100→240 保留 resize；MaterialEditDialog.tsx label 改"其他"+textarea（minHeight60/等宽/resize）。vitest 5 文件 19 用例全绿 + tsc EXIT 0 + build EXIT 0。改动记 docs/frontend-changes.md §反馈。后端联动点（#5 输出）由 backend-fb5 处理 | 前端 |
| 2026-08-13 | 架构师 | #7 3D 重合栅元检查契约存档（arch-fb7）：docs/contracts/geometry-check.md 已锁定。方案 A（FreeCAD 布尔求交 + AABB 预过滤），复用 worker 已建实体零成本；补集 #n 靠精确布尔零体积天然不误报；severity=info/warning/error 按 volumeFraction+含 void；上限 max_boolean_ops=300 超限 truncated；模块 seam=新 app/overlap_classify.py（stdlib）+ preview_cache.put/get_overlaps；独立端点 /api/check-overlap（25→26 闸门双向一致，否决污染 preview-3d 性能契约）；验收=5 组单测+契约闸门+vitest+性能契约零回归。本轮不施工，等待排期（P0/P1 完成后） | 架构师 |
| 2026-08-13 | 测试 | 反馈 #1/#6 根因复现确认（tester-fb1）：#1 FM5 全链无 multiplier（入口门 core.py:1001-1003 正则要求 F 后跟数字，FM5 落 other_cards；parse_f_tally 无 FM 分支；TallyDefinition 无 multiplier 字段）；#6 `_handle_text_to_section`（api_server.py:714）shell 把 cell 文本放曲面段后→落数据段→other_cards，用户全部栅元字段丢失。新增回归测试 test_regress_fm5_import.py + test_regress_cell_text_to_form.py（6 用例），基线 pytest 271→277。评估：两条均 P0 阻塞上线，建议打回后端 | 测试 |
| 2026-08-13 | 管理 | 用户 7 条反馈工单受理（P:\dekstop\新建 文本文档 (2).txt）：P0 #6 cell 文本→表单丢数据 + #1 FM 卡不识别；P1 #3 F 卡输入光标消失 + #5 材料 options 改名"其他"框 + #4 高级其他卡空间窄；P2 #2 */+ 前缀（初核已支持待补测）+ #7 3D 重合栅元预警（增强待方案）。已并行派发 tester-fb1（复现 #1/#6）/ frontend-fb1（排查 #3/#4/#5）/ arch-fb7（#7 方案） | 项目经理 |
| 2026-08-12 | 测试 | 重新打包复验（v1.6.3 带 GeometryTab 修复，新 exe 6645760B）：exe 启动 + 5001 探活（三端点 200/ok）+ 3D 预览出图（shield 1040 tri / prob41c 500+1252 tri，miss/hit 一致）；**材料下拉修复经 bundle grep 确认已打包**（dist 中 createPortal/document.body/zIndex1100/1200/rgba(15,15,40,0.97)/mat-cell-btn 全在，clampDropdownLeft 名被压缩属预期）；spec 自检过；清理无残留。`docs/qa-report.md` 更新 | 测试 |
| 2026-08-12 | 构建 | **重新打包 v1.6.3 带进 GeometryTab 材料下拉修复（packager 后台）**：commit 链 773742a（gui/src/components/GeometryTab.tsx，portal 修复 + clampDropdownLeft 纯函数）→ c6705cf（gui/test/portalPosition.test.ts 6 用例，PM 授权随功能提交）。5 步全成功：vite 2.79s / PyInstaller 1m24s / sidecar 替换 0.7s / tauri build（vite 3.13s + Rust 增量 25.77s）/ 部署 0.6s。产物部署 D:\MCNP\MCNP输入卡生成器（exe 6645760B 23:50 + python.exe 25035105B 23:50 + _internal/）。自检过：preview_cache.py 在位（7481B）、PyQt5/vtk 剔除、vendor/geouned 在、版本 1.6.3 未改、spec 未动。待 tester 复验 | 打包 |
| 2026-08-12 | 测试 | 打包产物验证（v1.6.3，D:\MCNP\MCNP输入卡生成器）：exe 启动 + sidecar 拉起 5001；HTTP 探活（mcnp-detect/xsdir-check/generate 全 200/ok）；3D 预览出图（shield_20m 1040 tri / prob41c 500+1252 tri，miss/hit 四字段一致，preview_cache/vtk 惰性/bound 打包后正常）；spec 修复生效（_internal/app/preview_cache.py 存在、PyQt5 排除）；验证后清理无残留。`docs/qa-report.md` 更新 | 测试 |
| 2026-08-12 | 测试 | 3D 预览性能修复步 6 复核（perf/preview3d）：后端全量 **271 绿 / 0 红**（251 零回归 + 新增 20：bound 8 / cache 8 / worker 4）+ 前端 vitest **4 文件 13 用例全绿**。契约验收全过（bound 区间/cache 指纹与逐出/worker 惰性 vtk/frontend 四模块）；纪律无降级（既有 251 零改动、未触碰 generator/parsers、路由表 0 变更、无新运行时依赖、测试不 import api_server/FreeCAD）；联调实测（真实 FreeCAD）：preview-3d 四字段 miss/hit 一致 + 命中后 cross-section 复用正常（shield_20m→STL 1040 tri→slices=1）。`docs/qa-report.md` 更新 | 测试 |
| 2026-08-12 | 修复/重构 | **3D 预览性能修复·前端施工完成**（perf/preview3d）：步 3 IPv6 修复（`gui/src/utils/api.ts` 127.0.0.1 单一常量，14 文件 ~25 处 `localhost:5001` → `apiUrl`，grep 归零）；步 4 抽 4 深模块 `gui/src/three/`（TickGrid 台账+完整 dispose+步长表扩 1e6 / renderGate dirty 按需渲染 / cellMaterial 默认 opaque / cameraParams farNear≤1e4）+ Preview3D.tsx 接线 + 几何归一化（translate 先于 computeBoundingBox，相机 reframe）；步 5 加载态遮罩"正在生成 3D 几何…" + 半透明查看开关（默认关，M0 真空 opacity 0 不变）；vitest 基建（`gui/test/` 4 文件 13 用例全绿，devDependency，独立于 pytest 门禁）。tsc/build 通过。改动清单 docs/frontend-changes.md | 前端 |
| 2026-08-12 | 文档 | **3D 预览卡顿修复契约已锁定**：`docs/contracts/preview3d-performance.md`（复现实测修订版，repro 数据回填）。反驳原诊断 4 项（前端 STL 解析 54ms 非主因 / 120s 超时不触发 / OCC 无崩溃 / bound 布尔不慢）；确认真凶 P0a 打开卡（preview_cache 缓存 + 加载态，命中 ≤1s）、P0b 交互卡（TickGrid/renderGate/cellMaterial 深模块 + 默认 opaque + dirty 渲染）、P0c 深度（computeCameraParams 归一化 + far/near ≤1e4）；P0d 新发现（IPv6 前端 127.0.0.1 / vtk 惰性 / bound 位移参数 shield 5300→2700）。含 7 fixture 测试映射 + 后端 pytest 新增 3 文件 + 前端 vitest 4 文件 + 施工 6 步分工 + 3 项 PM 开放决策（半透明默认/vitest devDep/冷启动目标）。ADR 已补 3 条 | 架构师 |
| 2026-08-12 | 修复/性能 | **3D 预览性能后端施工完成**（perf/preview3d）：全量 pytest **271 绿 / 0 红**（251 基线零回归 + 新增 20 用例）。步 1 `_compute_bound_from_surfaces` 按类型→extent 表重写（方向向量与基点合成角点，RCC 轴长不当坐标；shield_20m 5300→2700 / stress_bunker 2050 / inp01 13100 / inp09 3665）+ `app/preview_cache.py` 深模块（fingerprint/put/get/evict_dir/evict_lru，LRU 上限 3，缓存自有拷贝，纯 stdlib）+ `tests/unit/test_preview_bound.py` + `tests/unit/test_preview_cache.py`（commit 97de569）；步 2 worker vtk 惰性（顶层删 import vtk，native 回退分支按需导入，无 GQ/SQ 免 0.46s）+ `tests/integration/test_preview3d_worker.py` AST 断言（commit e65d423）；步 3 `_handle_preview_3d` 接线缓存（指纹命中免 FreeCAD，响应四字段逐字段不变）+ `_clear_stl_session` evict_dir 联动（commit dbab84e）。真实 FreeCAD 联调：未命中 0.68s → clear-stl（缓存存活）→ 命中 0.02s 缓存目录（KPI ≤1.0s 达成）；命中后 `_STL_SESSION` 指向缓存目录 `/api/cross-section` 复用正常（联调点 2）。改动清单 docs/backend-changes.md §B | 后端 |
| 2026-08-12 | 测试 | P1 终态复核（refactor/generator-tech-debt，基 996d11f）：全量 **251 绿 / 0 红** 确认（复跑稳定）。逐 F# 通过（F#7 模块顶 import / F#3 grep 归零 / F#4 `_build_sdef_parts` 合并且 pin 精确串不变 / F#5+F#6 kitchen-sink R1/R4 红→绿 g1==g2 字节恒定 len 2099==2099，5 项残留差异全消除 / F#1 `_apply_raw_override` 收敛 + 1145 门控 pin 绿）；纪律无降级（251 用例不变）、无新依赖、漂移闸门绿、`_wrap_long_lines`/`_generate_structured_distributions` 未入 diff、review_findings 7 项全 Resolved。`docs/qa-report.md` 标注 P1 终态。**全量计划（P0+P1+P2）完成** | 测试 |
| 2026-08-12 | 修复/重构 | **P1 技术债重构完成**（refactor/generator-tech-debt 分支，自 experiment/geouned 建）：全量 pytest **251 绿 / 0 红**（复跑 ×2 稳定），6 红全部转绿。F#7 bf0a2c7（pymcnp 提到模块顶部，test_f7_*×2 转绿）→ F#3 c774e56（函数内 import json/sys + E0DBG 清理，grep 归零，test_f3_*×2 转绿）→ F#4 52ca251（`_build_sdef_parts` 合并两分支，字节不变）→ F#5/F#6 4a e404172（`_generate_multi_source` 拆 5 函数 + SDEF_FIELD_SPECS 表驱动，字符化门 test_generator_multi_source.py 全绿）→ 4b/4c 018ced5（kitchen-sink R1/R4 红→绿：POS F-dist 重建 / sdef_extra 分布关键字去重 / SI V 型 / banners 注释 + 回放重发 / SI 值扁平化 / 字段序统一）→ F#1 1488aae（raw_overrides 收敛 `_apply_raw_override`，tally-key 门控保留，test_generator_overrides.py 28/28 绿）。重锚定：review_findings.json 7 项全 resolved + commit 索引；UI_ARCHITECTURE.md §5.2/§5.3/§6.4/§7/§7.1 更新；test_tech_debt.py 注释行号更新；PROJECT_MEMORY.md 本行 + §6。边界遵守：未碰 api.yaml / api_server 路由 / _wrap_long_lines / `_generate_structured_distributions` join。待 tester 复核 | 后端 |
| 2026-08-12 | 文档 | **P1 重构契约已锁定**：`docs/contracts/p1-refactor.md`（目标 251 绿/0 红，顺序 F#7→F#3→F#4→F#5+F#6→F#1）。F#5+F#6 拆 5 函数 + `SDEF_FIELD_SPECS` 表驱动，按 §0.5.5 复合根因清单逐项消除漂移（POS F-dist 分支重建 `POS=F D1` / sdef_extra 分布关键字去重 / `_parse_sisp_structured` SI 类型表加 V / 分布注释进 banners 词汇 + 回放重发 / SI 值扁平化 / 字段序统一）；`_generate_distribution_sdef` 一并改序。F#1 收敛为 `_apply_raw_override`（保留 1145 raw_tally 门控）。含施工顺序/每步验收/重锚定清单/风险预警。记忆文档 ADR 已补 3 条 | 架构师 |
| 2026-08-12 | 测试 | P0 第二轮复核（M1.4 门禁）：全量 **245 绿 / 6 红** 复核确认通过，放行 P1。minimal+3 样例 R1 全绿、F-B/C/D 转绿、129+73+7 零回归、无断言降级、P1 范围未触碰；6 红 = 4 技术债 F#3/F#7 + 2 kitchen-sink R1/R4（归 P1，残留差异纯化为多源 SDEF 漂移）。`docs/qa-report.md` 已更新 | 测试 |
| 2026-08-12 | 文档 | 重建 `app/UI_ARCHITECTURE.md`（7 节：边界图/启动链路/import-root/deck JSON 契约/raw_overrides/往返保真/技术债地图）。原 P2 产出因未 commit 被 `git checkout -- app/` 误清空（PM 回滚失误，非内容问题）；本次按 7 节结构复原，**全部行号已 Grep 重锚定到 F-A~F-E 修复后工作树**：raw_overrides 守卫 1069/1081/1107/1115/1131/1139/1147/1168 + 不一致变体 1156；`_wrap_long_lines` 1010；F#3 import json 631/671（原 622/660）；F#4 `_generate_single_source` 247；F#5 `_generate_multi_source` 375；F#6 源字段枚举 384-403/434-449/486-489；F#7 pymcnp import 110；E0DBG core.py 826-827/1018-1021、__init__.py 138-140、api_server.py 608-610。如实反映缺陷修复后行为（banners.py 新增、normalize_lines 剥 &、parse_f_tally 多粒子、_is_cell_line 大小写、validate_deck 解包、options 上移 M 头行）。 | 架构师 |
| 2026-08-12 | 修复 | 引擎缺陷修复 F-A~F-E 重新施工（app/ 回滚后）：F-A 方案 C（新增 `app/generator/banners.py` 冻结全部生成器节头词汇 + `split_sections` 拦截 + 生成器改调常量，R1 minimal+3 样例全绿）；F-B `validate_deck` 解包 CellRow；F-C `parse_f_tally` 多粒子逗号正则；F-D materials options 上移 M 头行；F-E `normalize_lines` 剥尾 & + 保留续行 $ 注释（prob41c 伴生修复）。可选翻新 F-F/F-G/F-H 已做（pin 翻新为正确行为断言）。全量 pytest **245 绿 / 6 红**（4 技术债 F#3/F#7 + 2 kitchen-sink R1/R4，P1 范围）。`docs/backend-changes.md`；`review_findings.json` 已重锚定；`app/UI_ARCHITECTURE.md` 基线为空文件需 PM 决策 | 后端 |
| 2026-08-12 | 测试 | P0 测试防线：`tests/`（unit 129 + parser 73 + integration 49，共 234 通过 / 17 失败——17 个全为技术债/往返缺陷的按设计先红 pin）；R1-R4 往返不变量；3 份 vendor 样例冒烟（prob41c/avr13/inp24）；F#1-F#7 联动测试；契约漂移闸门 7/7 通过；`docs/qa-report.md`；`dev-requirements.txt`。M1.4 未全绿（R1 头泄漏 + validate_deck CellRow 崩溃阻塞 P1） | 测试 |
| 2026-08-12 | 文档 | 补 `app/UI_ARCHITECTURE.md`（7 节：边界图/启动链路/import-root/deck JSON 契约/raw_overrides/往返保真/技术债地图）；新建 `docs/contracts/api.yaml`（OpenAPI 3.0，25 端点，每 path 带 operationId） | 架构师 |
| 2026-08-12 | 测试 | P0 测试防线第一轮：tests/ 全目录 + dev-requirements.txt（pytest>=8.0）+ 3 样例 vendor（prob41c/avr13/inp24，inp24 来自官方 REGRESSION 库）；234 通过/17 失败；M1.4 未全绿（F-A 头泄漏/F-B validate_deck 崩溃/F-C 多粒子计数丢失/F-D options 丢失/F-E & 续行污染） | 测试 |
| 2026-08-12 | 管理 | P0 首轮验收：M1.4 打回修复，派架构师出 F-A~F-E 修复契约（docs/contracts/bugfix-f1-f5.md） | 项目经理 |
| 2026-08-12 | 管理 | 上级复核引发分歧（245/6 与 234/17）：经权威基线测量定论 234/17（0 误判），分歧源于复核时机落在施工后未 commit 工作树；PM 裁决回滚 + 权威测量，上级最终裁决：契约有效、kitchen-sink SDEF 漂移归 P1 | 项目经理 |
| 2026-08-12 | 修复 | 引擎缺陷修复重新施工（按契约 §0.5）：F-A banners.py 方案 C（节头拦截 24 词汇+7 动态）、F-B _unwrap_cells、F-C 多粒子正则、F-D options 上移 M 头行、F-E 剥 & + 伴生续行 $ 注释修复、F-F/F-G/F-H pin 翻新；全量 245 绿/6 红 | 后端 |
| 2026-08-12 | 测试 | M1.4 复核通过：245/6 复跑稳定，门禁 §0.5.2 全过（minimal+3 样例 R1 全绿、F-B/C/D 转绿、129+73+7 零回归），无断言降级，P1 范围未触碰；kitchen-sink 残留差异纯化为多源 SDEF 漂移 = P1 F#5/F#6 验收基准 | 测试 |
| 2026-08-12 | 管理 | P0 验收通过（245/6 达成，P1 放行条件满足）；UI_ARCHITECTURE.md 因回滚误伤被清空，派架构师重建中 | 项目经理 |
| 2026-08-11 | 新增/重构 | 材料/栅元/计数双向文本↔表单互转（深模块 useSectionTextMode）；3D 预览优先解析文本栅元；删除基础/高级标签页冗余文本模式按钮；侧栏头像=应用图标；修复 E 卡续行吞 F 卡 | 前端/后端 |
| 2026-08-11 | 修复 | 截面拖拽平移 1:1 跟随鼠标（按 SVG 渲染尺寸而非 zoom 缩放）；打包文档内附带 README | 前端/后端 |
| 2026-08-11 | 修复 | C810 卡速查索引渲染为 markdown 表格（原为折叠纯文本） | 前端 |
| 2026-08-11 | 文档 | README 更新至 1.6.3（独立窗口、条件行、#n 补集、材料选择器、STL 截面） | — |
| 2026-08-11 | 构建 | 新增 `#/preview3d` 哈希路由（浏览器模式测试 3D 用） | 前端 |
| 2026-08-12 | 管理 | 启动"测试补防+技术债清偿+文档补齐"计划（P0 测试 tester 已派发 / P2 文档 architect 已派发 / P1 重构等 M1.4 全绿） | 项目经理 |
