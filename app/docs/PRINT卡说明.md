# PRINT 卡说明

> 来源：C810.pdf 第 3-149 ~ 3-151 页

## 格式

```
PRINT
PRINT x1 x2 ...
PRINT -x1 -x2 ...
```

| 用法 | 效果 |
|------|------|
| 无参数 | 所有输出表全打 |
| 正数表号 | 基础表 + 指定的表 |
| 负数表号 | 全打 - 排除的表 |

**无 PRINT 卡** → 缩减输出（short output）。

## 始终会打印的内容

- 输入文件列表
- 问题摘要（粒子产生/丢失）
- KCODE 周期摘要
- 记数结果（tallies）
- 记数涨落图（TFC）
- 标记为 basic / default / shorten 的表

## 表类型

| 类型 | 含义 |
|------|------|
| **basic** | 强制打印，关不掉 |
| **default** | 自动打印，可以用 PRINT 关掉 |
| **（空白）** | 可选，用 PRINT 数字开启，负数关闭 |

## 常用表号

| 表号 | 类型 | 内容 |
|------|------|------|
| 10 | basic | 栅元重要性 |
| 20 | basic | 栅元温度 |
| 30 | basic | 截面表 |
| 40 | default | 记数分析 |
| 50 | | 源系数与分布 |
| 60 | | 曲面系数 |
| 70 | | 栅元体积/质量、曲面面积 |
| 80 | | 材料组成 |
| 85 | | 电子射程与 straggling |
| 86 | | 重合探测器 |
| 90 | basic | 物理常数与编译选项 |
| 98 | basic | 强迫碰撞与指数变换 |
| 100 | default | f(x) 记数密度图 |
| 102 | default | 累积 f(x) 与记数密度图 |
| 110 | | 曲面积分 |
| 120 | | 重要性函数质量分析 |
| 126 | | 宇宙映射（重复结构） |
| 128 | basic | 各栅元粒子活动 |
| 130 | | 中子/光子/电子权重平衡 |
| 140 | | 核素活动 |
| 150 | | DXTRAN 诊断 |
| 160 | | 源分布频率表、面源 |
| 161 | default | 记数分箱分析 |
| 162 | default | f(x) 记数密度 |
| 170 | | 权重窗生成器摘要 |
| 175 | shorten | keff 按周期估计 |
| 178 | basic | keff 按批次大小估计 |
| 190 | | 权重窗生成窗口 |
| 198 | | ESPLT/TSPLT 重要性比率 |
| 200 | | 电子射程与 straggling 表 |

## 特殊说明

- **表 160/161/162** 绑定：关 160 则 161 和 162 也不出。正数 PRINT 下不会自动出，除非显式写 160
- **表 175** 关不掉，但 `PRINT -175` 可缩短为（每 100 周期 + 最后 5 个）
- **表 128**（宇宙映射）必须在**初始运行**开启，continue-run 开不了，而且会占存储
- **执行行优先**：命令行上的 PRINT 选项优先级高于 INP 文件里的 PRINT 卡
- **continue-run**：可以用 PRINT 恢复任何表，但表 128 例外

## 示例

```
PRINT 110 40 150
```
→ 基础表 + 表 40/110/150。表 160-162（默认）不出现。表 175 缩短版。

```
PRINT -70 -110
```
→ 全部输出，排除表 70 和 110。表 175 完整版。

```
PRINT 110
```
→ 基础表 + 表 110。表 175 缩短版。
