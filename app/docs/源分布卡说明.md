# MCNP 源定义与源分布卡（完整版）

> 来源：C810.pdf 第 3-53 ~ 3-79 页（Source Specification 全节）
> 覆盖：四种源模式的完整写法

---

## 一、MCNP 四种源模式总览

| # | 源类型 | 触发卡片 | 适用场景 |
|---|--------|---------|---------|
| 1 | **通用源**（General Source） | `SDEF` | 日常固定源：点源、面源、体源、自定义能谱 |
| 2 | **面源**（Surface Source） | `SSW`（写）+ `SSR`（读） | 两步计算：先把粒子存文件，再读出来当源（如轫致辐射接力） |
| 3 | **临界源**（Criticality Source） | `KCODE` + `KSRC`/`HSRC` | Keff 临界计算（裂变链式自持） |
| 4 | **用户自定义源** | `SOURCE` 子程序 | SDEF/SSR/KCODE 都没有时默认调用，自己写 Fortran |

**源必须定义以下粒子变量**：ERG（能量）、TME（时间）、UUU/VVV/WWW（方向）、XXX/YYY/ZZZ（位置）、IPT（粒子类型）、WGT（权重）、ICL（起始栅元）、JSU（起始曲面）。

---

## 二、通用源 SDEF — 写法详解（3-54）

### 基本格式

```
SDEF 源变量 = 规格 ...
```

**空卡默认**：14 MeV 各向同性点源，位置 (0,0,0)，时间 0，权重 1。

三种赋值方式：
| 方式 | 示例 | 含义 |
|------|------|------|
| 直接给值 | `SDEF ERG=14 POS=0 0 0` | 固定值 |
| 分布编号 | `SDEF ERG=D1` | 从分布1取样，需配 SI/SP 卡 |
| 依赖分布 | `SDEF ERG=FERG D1` | 分布1依赖ERG变量，需配 DS 卡 |

### 源变量表（Table 3.3）

| 变量 | 含义 | 默认 |
|------|------|------|
| CEL | 起始栅元 | 由位置自动确定 |
| SUR | 起始曲面（0=体积源） | 0 |
| ERG | 能量（MeV） | 14 |
| TME | 时间（shakes） | 0 |
| DIR | 方向余弦（相对VEC） | 体积源：各向同性；曲面源：余弦分布 |
| VEC | 方向参考矢量 | 体积源：无；曲面源：法线 |
| NRM | 曲面法线符号 | +1 |
| POS | 位置参考点 | (0,0,0) |
| RAD | 径向距离 | 0 |
| EXT | 沿AXS距离 / 与AXS夹角余弦 | 0 |
| AXS | EXT/RAD参考轴 | 无 |
| X, Y, Z | 笛卡尔坐标 | 未定义 |
| CCC | Cookie-cutter 栅元 | 无 |
| ARA | 平面源面积 | 无 |
| WGT | 粒子权重 | 1 |
| EFF | 取样效率阈值 | 0.01 |
| PAR | 粒子类型 1=N,2=P,3=E,4=F(正电子) | 由MODE卡 |
| TR | 源变换 | 无 |

### 各类源的具体写法

**点源**：
```
SDEF POS=2 3 4 ERG=14 PAR=2
```
（X Y Z 三个坐标都指定 → 点源）

**多个点源**：
```
SDEF POS=D1
SI1 L 0 0 0 10 0 0 20 0 0     ← 三个位置
SP1 0.25 0.5 0.25              ← 概率
```

**球体源**（POS+RAD）：
```
SDEF POS=0 0 0 RAD=D1
SI1 5 10            ← 内外半径，默认幂律 a=2 → 体积均匀
```
（RAD=0 → 点源）

**柱体源**（POS+AXS+RAD+EXT）：
```
SDEF POS=0 0 0 AXS=0 0 1 RAD=D1 EXT=D2
SI1 0 2          ← 内外半径，默认 a=1 → 体积均匀
SI2 -5 5         ← 沿轴范围
```

**笛卡尔体积源**（X+Y+Z）：
```
SDEF X=D1 Y=D2 Z=D3
SI1 -2 2
SI2 -2 2
SI3 -2 2
```

**栅元体积均匀源**（CEL）：
```
SDEF CEL=2       ← 栅元2内体积均匀
```

**曲面源**（SUR）：
```
SDEF SUR=1 POS=0 0 0 RAD=D1   ← 平面源，POS必须在面上
SDEF SUR=5 AXS=0 0 1 EXT=D2   ← 球面源（AXS+EXT组合暗示球面）
```

**指定能谱**：
```
SDEF ERG=D1
SP1 -3            ← Watt 裂变谱（a=0.965, b=2.29）
SP1 -2            ← Maxwell 裂变谱
SP1 -4            ← 高斯聚变谱
```

**指定方向**：
```
SDEF DIR=1 VEC=0 1 0       ← 沿Y轴单方向
SDEF DIR=D1 VEC=0 1 0      ← 方向分布
SP1 -31 1.5                ← 指数偏倚
```

---

## 三、源分布卡 — SIn / SPn / SBn / DSn / SCn（3-63）

### SIn — 源信息卡

```
SIn H I1 ... Ik        （H = 直方图：分箱边界，单调递增）
SIn L I1 ... Ik        （L = 离散值：栅元号、谱线能量）
SIn A I1 ... Ik        （A = 概率密度点，单调递增）
SIn S I1 ... Ik        （S = 分布编号，先选分布再取样）
```

### SPn — 源概率卡

```
SPn D P1 ... Pk        （D = 分箱概率，默认）
SPn C P1 ... Pk        （C = 累积概率）
SPn V P1 ... Pk        （V = 按体积加权，仅 CEL）
SPn f a b              （f = 内置函数编号）
```
概率不需要归一化。

### 内置函数表（Table 3.4）

| 函数号 | 变量 | 名称 | 说明 |
|--------|------|------|------|
| -2 | ERG | Maxwell 裂变谱 | p(E)=C·E^½·exp(−E/a)，默认 a=1.2895 |
| -3 | ERG | Watt 裂变谱 | p(E)=C·exp(−E/a)·sinh(√(bE))，默认 a=0.965, b=2.29 |
| -4 | ERG | 高斯聚变谱 | p(E)=C·exp[−((E−b)/a)²]，b=-1=DT |
| -5 | ERG | 蒸发谱 | p(E)=C·E·exp(−E/a)，默认 a=1.2895 |
| -6 | ERG | Muir 速度高斯 | 速度空间高斯 |
| -21 | DIR/RAD/EXT | 幂律 | p(x)=c|x|^a |
| -31 | DIR/EXT | 指数 | p(μ)=c·e^(aμ) |
| -41 | TME/X/Y/Z | 高斯 | 半高宽 a、均值 b |

### SBn — 源偏倚卡
```
SBn D B1 ... Bk       （偏倚概率）
SBn -21 a  /  SBn -31 a   （仅这两个函数）
```
偏倚抽样加快收敛，权重自动补偿。

### DSn — 依赖源分布卡
```
DSn H J1 ... Jk       （直方图）
DSn L J1 ... Jk       （离散）
DSn S S1 ... Sk       （分布编号）
DSn T                 （匹配模式）
DSn Q V1 S1 V2 S2 ... （分段区间→分布）
```

### SCn — 源注释卡
```
SCn 注释文字
```

> 解析器支持（2026-08-16）：SCn 属于分布家族，SDEF 后收集链不再被 SCn 打断；
> 注释文字结构化进分布条目 `sc` 字段，生成器回放 `SCn 注释文字`（inp02.i 实卡验证）。

---

## 四、面源 SSW / SSR — 写法详解（3-70）

### 第一步：SSW 写面源文件

```
SSW S1 S2 (C1 ... Ck) S3 keyword=values
```

| 参数 | 含义 |
|------|------|
| Si | 记录粒子穿过的曲面编号（宏体曲面不允许） |
| Ci | 栅元编号：正=进入该栅元，负=离开该栅元 |
| SYM | 0=无对称（默认）；1=球对称（只能一个球面）；2=双向记录 |
| PTY | N/P/E 记录粒子类型，缺省=全部 |
| CEL | 临界计算中记录裂变源的栅元 |

**示例**：
```
SSW 4 -7 19 (45 -46) 16 -83 (49)
```
曲面19记录：进入45或离开46的粒子；曲面83记录：进入49的；曲面4/7/16：全部。

### 第二步：SSR 读面源文件

```
SSR keyword=values ...
```

| 关键字 | 含义 |
|--------|------|
| OLD S1 S2... | 读原文件中的哪些曲面 |
| CEL C1 C2... | 读原文件中的哪些裂变栅元 |
| NEW S11 S12... | 新问题中粒子从哪些曲面出发（面→面映射） |
| PTY | 读哪些粒子类型 |
| COL | -1=仅无碰撞直接粒子；1=仅碰撞后粒子；0=全部 |
| WGT x | 每个粒子权重乘 x |
| TR n / TR Dn | 变换：把面源变换到新位置（多个位置用分布） |
| PSC c | 探测器/DXTRAN 用的角度分布幂次（0=各向同性） |

**球对称专用**（原 SSW 用 SYM=1）：AXS、EXT、POA、BCW。

**示例 1：变换面源到多个位置**
```
SSR OLD 3 2 NEW 6 7 12 13 TR D5 COL 1
SI5 L 4 5
SP5 .4 .6
SB5 .3 .7
```

**示例 2：球对称源偏倚**
```
SSR AXS 0 0 1 EXT D99
SI99 -1 .5 1
SP99 C .75 1
SB99 .5 .5
```

**示例 3：三束交汇**
```
SSR OLD 2 TR=D1 WGT 6.0
SI1 L 11 22 33
SP1 1 1 1
TR11 ...
TR22 ...
TR33 ...
```

### 面源注意事项
- 记录方向只有单向（除 SYM=2）
- **NPS 改变会重新分配权重**：NPS **变小**→按比例**拒绝**轨迹；NPS **变大**→轨迹**复制**且每条权重减半（例：100→200，每条复制成 2 条各半权重）
- 探测器/DXTRAN 需要 PSC 近似，靠近源时可能 WRONG
- 面源文件默认名 WSSA（异常终止时是 WXXA，需与 RUNTPE 一起保存）

---

## 五、临界源 KCODE — 写法详解（3-77）

### KCODE 卡

```
KCODE NSRCK RKK IKZ KCT MSRK KNRM MRKP KC8
```

| 参数 | 含义 | 默认 |
|------|------|------|
| NSRCK | 每周期源历史数 | 1000 |
| RKK | 初始 Keff 猜测 | 1.0 |
| IKZ | 跳过（不累计记数的）周期数 | 30 |
| KCT | 总周期数 | IKZ+100 |
| MSRK | 分配源点存储 | 4500 或 2×NSRCK |
| KNRM | 0=权重归一 / 1=历史数归一 | 0 |
| MRKP | MCTAL/RUNTPE 保存周期数 | 6500 |
| KC8 | 0=全部周期 / 1=仅活跃周期 | 1 |

### KSRC — 初始源点

```
KSRC x1 y1 z1 x2 y2 z2 ...
```
- 每个可裂变区域至少一个点
- 点必须远离栅元边界
- 至少一个点必须在含裂变材料的栅元中
- 初始能量从 Watt 谱取样（a=0.965, b=2.29）
- 缺省：用 SRCTP 文件或 SDEF 卡提供初始源

### HSRC — 香农熵网格

```
HSRC nx xmin xmax  ny ymin ymax  nz zmin zmax
```
- 评估裂变源收敛性的 3D 网格
- 每方向 5~10 个格点为宜
- 缺省：MCNP 自动确定（最少 4×4×4）

### 完整临界计算写法
```
MODE N
KCODE 10000 1.0 50 150
KSRC 0 0 0 5 5 5
```

---

## 六、用户自定义源 SOURCE 子程序（3-79）

当 SDEF/SSR/KCODE 都缺省时，MCNP 调用用户提供的 `SOURCE` 子程序。

```
SUBROUTINE SOURCE
    ...  用户代码设置源变量 ...
    XXX = ...   ! 位置
    YYY = ...
    ZZZ = ...
    ERG = ...   ! 能量
    WGT = ...   ! 权重
    IPT = ...   ! 粒子类型
RETURN
END
```

要点：
- 方向余弦 UUU/VVV/WWW 默认已设为各向同性，需要各向同性就不用管
- 随机数用 `RANG()` 生成 0~1
- 数据可用 IDUM/RDUM 卡传入（各最多50项）
- 探测器/DXTRAN + 各向异性源需要额外写 `SRCDX` 子程序
- 备用数组 `SPARE(M)`（M=1~3）可自由使用，MCNP 不重置

---

## 七、示例汇总

### 示例1：带能量分布的均匀点源
```
SDEF ERG=D1 POS=x y z WGT=w
SI1 H E1 E2 ... Ek
SP1 D0 P2 ... Pk
```

### 示例2：球面源 + 指数偏倚
```
SDEF SUR=m AXS=i j k EXT=D6
SB6 -31 1.5
```

### 示例3：向内发射的球面源
```
SDEF SUR=m NRM=-1 DIR=D1 WGT=w
SB1 -21 2
```

### 示例4：体积均匀源（Watt谱）
```
SDEF ERG=D1 POS=x y z CEL=m RAD=D2 AXS=i j k
SP1 -3
SI2 -3 3
```

### 示例5：单方向平面源 + cookie-cutter
```
SDEF SUR=m POS=x y z RAD=D1 DIR=1 CCC=n
SI1 r
```

### 示例6：两位置点源，各自不同能谱
```
SDEF POS=D1 ERG=FPOS D2
SI1 L 5 3.3 6 75 3.3 6
SP1 .3 .7
DS2 S 3 4
```

### 示例7：直线均匀源 + 变换
```
SDEF DIR=1 VEC=0 0 1 X=D1 Y=0 Z=-2 TR=1
SI1 0.0 0.5
SP1 0.0 1.0
TR77 0.5 0.5 0 0.4 0.3 0 -0.3 0.4 0
```

### 示例8：高斯束 + cookie-cutter + 三束变换
```
SDEF DIR=1 VEC=0 0 1 X=D1 Y=D2 Z=0 CCC=999 TR=D3
SP1 -41 .470964 0        ← X高斯，FWHM=.47
SP2 -41 .235482 0        ← Y高斯，FWHM=.235
SI3 L 11 22 33
SP3 1 2 3
SB3 1 1 1
TR11 0 0 -2 1 0 0 0 1 0 0 0 1
TR22 -2 0 0 0 1 0 0 0 1 1 0 0
TR33 0 -2 0 .707 0 .707 .707 0 -.707 0 1 0
```

### 示例9：面源两步法
```
! 第一步：写面源
MODE N P
SSW 2 3
NPS 100000

! 第二步：读面源（新文件）
MODE N
SSR OLD 2 3 WGT 1.0
NPS 200000
```

### 示例10：临界计算
```
MODE N
KCODE 10000 1.0 50 150
KSRC 0 0 0
HSRC 10 -100 100 10 -100 100 10 -100 100
```

---

*完 — 覆盖 MCNP6 手册 3-53 ~ 3-79 页全部源定义内容。*
