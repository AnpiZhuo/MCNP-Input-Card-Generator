"""
app — MCNP Input Card Generator Application Package

This is the root package of the PyQt5-based MCNP input card generator.
It contains all sub-packages for the GUI (tabs, dialogs, widgets),
the MCNP installation detector, and the card generator engine (parser,
validator, generator).
"""

# Make key classes/functions available at the app package level (if needed)
# from .mcnp_detector import detect_mcnp
