"""
app — MCNP Input Card Generator Application Package

This is the core Python package shared by the Tauri/React frontend's backend
(api_server). Contains the card generator engine (parser, validator,
generator), the FreeCAD/GEOUNED STEP conversion pipeline, and shared helpers.
"""
